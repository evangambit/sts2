# Divergence catalogue

Every emulator/game divergence found by differential testing, with the metric that
exposed it, the cause, and the seed to reproduce it on. New entries go at the
bottom of their table; nothing is removed once fixed, because the *reason* a bug
survived as long as it did is usually more reusable than the fix.

Two companions: `tests/fixtures/run_trace/README.md` lists the captured runs and
their current status, and `docs/replay-verification.md` explains why fidelity is
measured as "% of real runs replayed exactly" rather than claimed as "no bugs".

## How to reproduce

```bash
uv run python scripts/replay_full_run_trace.py tests/fixtures/run_trace/<SEED>-a8.json
```

The seeds below are all ascension 8 on **v0.107.1** (build 23811903). "Step N" is
an index into that trace. A few entries were found by the per-encounter sweep
instead:

```bash
uv run python scripts/combat_sweep.py --seeds <SEED> --encounters <name> --ascension 8 --play
```

Seeded capture requires custom mode — standard mode refuses a chosen seed — and a
run's FIRST act is seed-dependent, so roughly half of all seeds open in Underdocks
rather than Overgrowth.

---

## Open

Seven. Two are in act 2 — territory nothing could reach until buffed runs started
winning, and which `--enter-acts` now puts a few seconds away instead of behind a won boss
fight. O20 is a Glory monster power, recorded rather than approximated on purpose: a buff
nothing reads is what `BuffId.Ebb` turned out to be. Its sibling O19 turned out not to be
a gap at all — see E127. **The act-2 ancients are finished**: all sixteen blessings do what they should when
taken, all four of their enchantments behave in a fight, and Clone's rest option is
modelled. The third (O17) is act-agnostic and is the tail of the event work in E76-E78.

**O11 is gone**, and it was a harness bug in both halves (H15, H16) — the emulator was
right about the Fogmog all along. The capture that reproduced it is committed as
`7WGQ2VNJ4M-a8-buff400`, the only trace that walks a Fogmog's illusion.

**Read "three" narrowly.** This table holds divergences a capture or a test SURFACED and
nobody has closed. It is not a measure of what is wrong with the emulator, and right now
it is a bad proxy for one: the largest known defect surface is not in it at all. That is
**no suspect ascension literals left** (`scripts/audit_ascension_literals.py` reports
zero), **no encounter left unwalked** (`CombatCoverageTests.Pending` is empty), and **three whole characters — Defect,
Necrobinder, Regent, 264 cards — with not one card verified**. Those are known-wrong or
never-checked rather than merely unlisted; every batch put through the combat suite so far
has found something. HANDOFF's "Next work" is the register for that, and it is the one to
read before deciding what to do next.

| # | Metric | Seed | What is known |
| --- | --- | --- | --- |
| O16 | Sea Glass's grid is a 0-to-15 multi-select; the emulator's hands over picks until they run out | `AncientBlessingTests` | `CardSelectorPrefs(prompt, 0, list.Count)` is MinSelect 0, MaxSelect 15, with a manual confirm — the player may take any number of the fifteen, including none. The emulator's offer grid takes `PendingOfferPicks` cards and cannot stop early. A run that wants all fifteen matches; one that wants three does not. The same gap applies to any future 0-to-N grid. |
| O17 | Mad Science plays as a plain 12-damage attack whatever Tinker Time built | `ZenWeaverReflectionsTinkerTests` | The event is faithful now — three pages, the right shuffles, and the chosen type and rider recorded on the `CardInstance` — but the CARD is not. `MadScience.OnPlay` branches on its type (attack 12 / block 8 / a power) and then applies one of nine riders; the emulator has the card-table entry, so it plays as the Attack row regardless. Two of the riders need powers that do not exist (`CuriousPower`, `ImprovementPower`) and Chaos plays a mocked random card, so this is a card-implementation job rather than a missed line. |
| O18 | `RampartPower`'s standing total, and whether it stops when the shield dies | `TurretOperatorTests` | The grant reads the max Rampart over LIVING enemies, so a dead Living Shield should stop arming the Turret Operator — and the emulator still hands it 25 with the shield dead. The accumulation is odd too: 50, 50, then 25 across three turns, where the rule reads as 25 each player turn against a block that clears on the enemy's own turn. **Neither has ground truth**: a live capture compares intents and player HP, not enemy block, and the sweep's policy attacks the shield first so it never outlives the turret. The test asserts only that the turret is armoured while the shield lives; pinning a number this cannot justify is how a wrong reading gets fixed in place. |
| O15 | Alchemical Coffer wants six potion slots; the emulator caps at three | `AncientBlessingTests` | `RunState.PotionSlots` is a fixed `int[3]`, as are `CombatState`'s and `GameState`'s, and the combat observation is laid out around that width — so a run cannot hold more than three potions whatever `MaxPotionSlots` says. The Coffer grants four slots on top of two. Widening the array is an observation-layout change, which is why it is recorded rather than done in passing; the test deliberately does NOT assert three, because that would bake the cap in as though it were the rule. |
| O20 | `WitheringPresencePower` is unmodelled, so the Aeonglass's Withers only come from its own move | `AeonglassTests` | `AfterAddedToRoom` gives every opponent a `WitheringPresencePower` with a CardsLeft counter of 6: every sixth card the player plays puts a Wither in their HAND, and the Aeonglass's `WitherUpgradeCount` fake-upgrades each one to match how many times INCREASING_INTENSITY has run. The intensity move's own Withers are modelled; this steady drip is not, and it is the larger source over a long fight. |
| O23 | whether The Hunt's extra card reward populates BEFORE or AFTER the room's ordinary one | `TheHuntTests` | A Hunt that kills adds a whole extra `CardReward` to the room. `CardReward` does not roll its cards in the constructor — `Populate()` does, when the screen is built — so the extra offer's three options come off `PlayerRng.Rewards` with the rest rather than at the moment of the kill. What is NOT settled is the order: both rewards are `RewardsSetIndex` 5, so they populate in the order they were added, and the extra is added DURING the combat while the ordinary set is built at its end. Modelled as "after", which is a guess; every card the rewards stream produces from the second offer on depends on it. A capture of a run that plays The Hunt to a kill settles it in one fight. |
| O25 | a Nightmare's promise is invisible in the observation | `NightmareTests` | `NightmarePower` is a real, visible power in the game, and its `StringVar("Card")` names the card it is holding — so a player can see that three Backstabs are arriving next turn and plan the energy for them. The emulator carries the clones in `CombatState.CopiesToHandBeforeDraw`, which nothing in `CombatObservation` reads, so an agent pays three energy for an effect it cannot observe and then discovers the hand next turn. `ReturnToHandBeforeDraw` has the same gap and predates this. Recorded rather than fixed because exposing it is an observation-layout change, the same reason O15 stands. |
| O26 | orbs are absent from the observation entirely | `OrbTests` | `CombatObservation` has no orb field of any kind — not the ring, not the types in it, not Dark's banked value or Glass's remaining charge. A Defect agent cannot see the mechanic its whole character is built on, so it cannot learn when to evoke, and 88 cards' worth of correctness buys nothing until this is fixed. Recorded rather than done because it is an observation-layout change, the same reason O15 and O25 stand — but unlike those two this one is load-bearing for a whole character, and it should be the FIRST Defect work rather than the last. |

All fourteen encounter models that roll their own composition now read the encounter's
own stream, and `EncounterCompositionStreamTests` holds them there by varying the seed and
watching the roster — the only check that separates a builder reading the stream from one
that quietly fell back (E90).

All thirty-two committed traces replay with no divergence on any compared field, the two
stand-in draw counts E30 left behind are gone, and **every one of the twenty-five Neow
blessings the screener knows about has now been captured and replayed**.

That is a stronger statement than the last time this table was empty: twenty of the
thirty-two were captured specifically to walk paths the others did not — most of them by
screening seeds for a Neow blessing no committed trace had taken — and closing what they
found took twenty-seven engine fixes. It is still a statement about these thirty-two runs,
not about the emulator: the last nine captures were taken after the set had gone green and
eight of the nine found something. The blessing seam is now exhausted, which means the next
capture has to be chosen on some other axis — an act 2, an unwalked event, a fight nobody
has lost yet. See the note under "Patterns" about what a green
set does and does not measure.

**The thirty-second capture is the argument for that, taken on a different axis and worth
copying.** `7WGQ2VNJ4M-a8-buff400` was taken to settle O11 rather than to cover a
blessing, and it reproduced the divergence at the same step the original had — which is
what made a bug that had survived two wrong hypotheses solvable in one reading. If a
catalogued suspicion resists, **re-capture rather than re-reason**: the trace is cheap
next to the staring, and this one killed the recorded hypothesis on the first look.

The two that stood here last are E22 to E26 below; E27 and E28 came out of reading the
code they touched, not out of a capture. What closed them is worth knowing
before opening the next one: **O2's stated cause was right and its stated location was
wrong.** The shuffle stream really was at a different position — but the fight it
surfaced in had nothing to do with it, and the draws were spent in a fight three floors
earlier that had already been won.

### How to measure a stream position on the live side

The game exposes no call counts, but a capture pins them anyway. A combat's opening pile
is `hand_ordered + draw_pile_ordered`, its input is the deck in run order, and the shuffle
is a plain `UnstableShuffle` — so the position the game was at is the one `k` for which
`shuffle(deck, stream[k:])` reproduces the capture. Sixteen cards make a false match
vanishingly unlikely, so a search over `k` reads the answer straight off a fixture, with
no game running. The emulator's side is `Sts2Run_GetShuffleRngCallCount`, which already
existed. Comparing the two per combat named the fight in one pass, where reading the fight
that failed could not have.


## Fixed — engine

| # | Metric | Seed / where | Cause |
| --- | --- | --- | --- |
| E1 | Endless Conveyor upgraded a Defend where the game upgraded a Strike | event captures (`ABCDEF`) | **`EventRng` built a fresh stream per call**, so every draw inside an event came off position 0. The game gives an event one `base.Rng` for its lifetime: the conveyor rolls its dish in `CalculateVars`, then Observe the Chef's pick reads the SECOND value. |
| E2 | (same fix) tribute cost, bridge card, Ranwid's relic and the trader's shelf changed between reads | event captures | Those values were **accessors that re-rolled on every call** — idempotent only while every draw restarted. The game computes each once as the event is generated. Ranwid also never spent his potion draw, so the relic came off the wrong slot. Consequence: **reading the action mask advanced the run's randomness**, so an agent perturbed the outcome it was deciding about. `EventRngStabilityTests` pins that invariant. |
| E3 | `state_type` step 1: game `event`, emulator `map` | `4KJ7X2MQND` | **Neow's blessings had no Proceed page.** Every one ends in `AncientEventModel.Done()`, a `SetEventFinished`. Only Kaleidoscope was given one, because its reward screen made the gap visible; the rest jumped to the map and swallowed a real action. |
| E4 | `battle.enemies` step 105: enemy at 29 HP live, 24 here | `Y75EFT6EDV` | **A multi-hit attack retargeted.** `AttackCommand` re-filters its targets each hit and BREAKS when none are alive; for a single-target attack that list is just the one chosen, so a target that dies partway through eats the remaining hits. `DealDamageMultiHit` re-resolved the target per hit and fell back to the first living enemy. Affects every multi-hit single-target attack. |
| E5 | `event.event_id` step 59: game `SUNKEN_TREASURY`, emulator `WAR_HISTORIAN_REPY` | `4KJ7X2MQND` | **`IsEventAllowed` ends in `_ => true`**, so an event whose rule nobody transcribed is silently ALLOWED. War Historian Repy's rule is `return false` — never drawn from the sequence at all. Reading all 35 overrides also found: Endless Conveyor wanted 40 gold (game: 120), Waterlogged Scriptorium's `or Deck.Count > 0` clause made a 55-gold test always pass, Unrest Site was unconditional (game: hp ≤ 70% of max), and Whispering Hollow, Trash Heap, Spiraling Whirlpool, Punch Off and Slippery Bridge had no case at all. |
| E6 | `battle.enemies` step 68: gardener block 7 live, 0 here | `4KJ7X2MQND` | **Skittish unmodelled.** Every Phantasmal Gardener carries it: the first card each turn to land UNBLOCKED damage gives it `SkittishAmount` block (7 at A8, 6 below). The power persists — spent for the turn, cleared when the player's turn ends. |
| E7 | Soak crash: "Cannot rewind an RNG stream" | soak, boosted runs | `RollDish` fast-forwarded a fresh stream to grabs-minus-the-fifths, a workaround from when every `EventRng` call built its own. With a persistent stream that became a REWIND as soon as anything else drew from the event. **The C# suite could not catch this** — it walks the belt down one path and never mixes a grab with another draw. |
| E8 | `player.hp` step 77: 61 live, 58 here (via block 8 vs 5 at 74, deck at 63) | `DPUJR117FL` | **Fishing Rod was a constant with no effect.** It upgrades a card every third MONSTER room — elites, bosses and event-started fights return early and do not even advance its counter — picking off `Rng.Niche`. Entering an event room now also marks the room type, so a fight-shaped event cannot be counted as an ordinary combat. |
| E9 | (same fix) the event sequence could run out | — | `RoomSet.NextEvent` is `events[visited % count]`: a **ring**, not a list. `EnsureNextEventIsValid` also skips events the run has already SEEN, not just disallowed ones. Neither was modelled. |
| E10 | `battle.enemies` intent step 18: 14 live, 11 here | `4KJ7X2MQND` | **CorpseSlug's WHIP_SLAP was folded into a single 6** rather than `MultiAttackIntent(3, 2)`. Identical while the slug has no Strength — and Ravenous is how it gets some. The game adds Strength to EACH hit. |
| E11 | `battle.enemies` intent step 18: 14 live, 16 here | `4KJ7X2MQND` | **Ravenous applied 5**, the A9 branch of `GetValueIfAscension(DeadlyEnemies, 5, 4)`, as a bare literal. At A8 it is 4. Exactly the bug class `Ascension.cs` was written to prevent. |
| E12 | `battle.enemies` intent step 24: game announces Attack 8, emulator announces no attack | `4KJ7X2MQND` | **Intent order.** Every `MoveState` declares its attack FIRST and the debuff/buff second, and the readout follows. Sludge Spinner's OIL_SPRAY and Living Fog's ADVANCED_GAS were modelled as a "Debuff carrying damage", so an agent reading intent types saw a debuff where the game shows an attack. Living Fog's `Smoggy` was also never applied. |
| E13 | `event.event_id` step 115: game `AROMA_OF_CHAOS`, emulator `SUNKEN_STATUE` | `DPUJR117FL` | **Neow's room consumes an event slot.** `MapPointType.Ancient` maps to `RoomType.Event`, and `RunManager` marks every room it enters — so `eventsVisited` is already 1 before the first real event. `NextEvent` is `events[visited % count]`, so the first event a run meets is `events[1]`. We started the cursor at 0. Confirmed by replaying into the live game and reading `events_visited` off the save. |
| E14 | `player.deck` step 118: game took Stone Armor, emulator took Expect a Fight | `DPUJR117FL` | **An event transforms off its own stream.** `CardCmd.TransformToRandom` takes the rng to roll with, and every EVENT passes its own `base.Rng`. Only New Leaf — which does not use the deck-selection path — uses `Rng.Niche`. We rolled all of them off Niche. |
| E15 | `player.hp` step 230: 24 live, 0 here (the run ends) | `DPUJR117FL` | **Fairy in a Bottle unmodelled.** An Automatic potion whose `ShouldDie` returns false for its owner; `AfterPreventingDeath` heals `Max(MaxHp × 0.3, 1)`. Death now resolves through one `PlayerIsDead` helper instead of four scattered `PlayerHp <= 0` checks. |
| E16 | sweep turn 4: player hp 53 live, 45 here | `QS2GYXRKWN`, `--encounters living-fog --play` | **The Living Fog's bomb takes a slot in FRONT of it.** A live capture lists `[Gas Bomb, Living Fog]`; we appended, so the fog came first and the same target index named a different creature on each side. The HP consequence is **not confirmed** — `combat_sweep --play` derives each action from the emulator's own state, so changing the roster order walks the two sides out of step. |
| E17 | `state_type` step 73: game `event`, emulator `rewards` | `41TJ3T2Y0Q` | **Brain Leech's reward belongs to its event.** Once the reward screen empties the run returns to the event's finished page, like Neow's. The check for that sat BELOW the reward-screen return, which exits first, so it never ran. |
| E18 | `state_type` step 1: game `rewards`, emulator `card_reward` | `CF32ERF3DH` | **Lost Coffer** is a `RewardsCmd.OfferCustom` of TWO rewards — a three-card CardReward and a PotionReward — so both sit on a SCREEN to be claimed. We handed the card reward straight over. The potion is guaranteed, not rolled, so it no longer spends 0.1 of the odds. |
| E19 | `battle.enemies` intent step 68: game announces a Stun, emulator announces Attack 12 | `CF32ERF3DH` | **Ravenous's stun did not reach the readout.** When an ally dies the survivor takes Strength and skips its next move; the intent changes immediately. We applied both buffs and left the old intent up, telling an agent to expect 12 damage from a creature about to sit the turn out. |
| E20 | `battle.enemies` step 69: game has 2 enemies, emulator has 1 | `QD1DQCJU2K` | **Seapunk's normal variant.** `SeapunkWeak` is one Seapunk; `SeapunkNormal` is a Calcified Cultist AND a Seapunk. Two encounters in the game, one entry here — like `CorpseSlugs` — so the variant must come off the weak flag, and did not. |
| E21 | `battle.enemies` step 111: split gremlins 15/18 live, 12/17 here | `WK1DEGZD8P` | **Reinforcements rolled off the wrong stream.** `CombatState.CreateCreature` calls `SetUniqueMonsterHpValue` for EVERY enemy it makes, spawned or not: HP comes off `Rng.Niche` and must differ from the other enemies on that side. The Gremlin Merc's two reinforcements used the combat rng with no uniqueness. |
| E22 | `state_type` step 23: game `monster`, emulator `map` — the run stalls on the map (was **O1**) | `KFMKQQA7MS` | **Winged Boots' free travel was unmodelled.** `MapTravel.GetTravelablePointsFrom` answers with `GetPointsInRow(row + 1)` — the WHOLE next row — while `Hook.ShouldAllowFreeTravel` holds, and `WingedBoots.ShouldAllowFreeTravel` is `!IsUsedUp`. `AfterRoomEntered` spends a charge only when the node just entered was not a child of the one left behind, so a run can hold the relic for a whole act without moving its counter. The map was right all along: all 77 edges matched. Two details that are not guesses: the boss is NOT in the game's `Grid`, so `GetPointsInRow` never returns it and `NMapScreen` makes it travelable outright from the last grid row instead; and the counter belongs on the RELIC (`[SavedProperty] TimesUsed`) — `RunState.WingedBootsTimesUsed` existed, was read by nothing, and is gone. |
| E23 | `player.hand` step 125: a fight's opening hand differs (was **O2**) | `WK1DEGZD8P` | **A combat the enemy phase ended kept playing.** `CombatManager.ExecuteEnemyTurn` awaits `CheckWinCondition` after EVERY enemy and returns the moment `IsInProgress` goes false; the emulator checked only at the very end of `EndTurn`, so it first ran a whole player turn that never happens — drew a hand, and reshuffled a 13-card discard pile to find one. `Rng.Shuffle` is a RUN-level stream, so those twelve draws left it ahead of the game's for the rest of the run and every later fight was dealt from the wrong position. The fight that exposed it was three floors later; the fight that caused it had ended, at floor 12, when the last Fat Gremlin took its Heist gold and fled on the enemy turn. Player death is the same rule and was the same bug: a fight that killed the player kept taking turns, which is how a seventh-turn relic fired in a fight that ended on the fourth. |
| E24 | `player.hp` step 132: 61 live, 58 here | `WK1DEGZD8P` | **Suck fired per hit.** `SuckPower.AfterAttack` runs once for the whole `AttackCommand` and grants `Amount x` the number of hits that dealt unblocked damage — so the Strength it hands over cannot reach the later hits of the attack that earned it. Applying it inside each hit gave a Fossil Stalker's two-hit Lash a second swing three points higher than the game's. Every multi-hit enemy attack now goes through one `DealAttack(enemy, state, damage, hits)` that counts the landed hits and triggers Suck after. |
| E25 | `player.block` step 137: 3 live, 5 here | `WK1DEGZD8P` | **Fossil Stalker's TACKLE never applied its Frail.** TACKLE_MOVE is `SingleAttackIntent` plus `DebuffIntent`, so the readout calls it an Attack — but the emulator resolved it in the DEBUFF branch, which its Attack intent never reaches. The Grasping Vines class again (E12), and the tell is the same: a move that attacks AND does something else resolves where its PRIMARY intent says. Every Defend after it blocked five where the game blocked three. |
| E26 | `battle.enemies` step 161: game `[22, 19]`, emulator `[19, 22]` | `WK1DEGZD8P` | **A summoned rat always joined the front.** `TwoTailedRat.CallForBackup` takes `Slots.LastOrDefault(s => no creature holds s)`, and the rats start in `Slots[2..4]` of five — so with the pack intact the last free slot is "second" and the newcomer really does lead. Once a rat has died its slot is free again: with "fifth" empty the newcomer joins the BACK. Hardcoding the front swapped the two survivors, and every target index after it named the other creature. `EnemyState.Slot` now carries the encounter slot, and the roster is ordered by it. The old guard (refuse when the roster already holds five rats) counted the DEAD, which the game's slot search does not. |
| E27 | Flame Barrier retaliates against nothing that has a special case | source read, no capture | **The retaliation was in the attack branch's generic tail**, past eighteen `break`s and past the multi-hit path's own. So it answered only single-hit attacks by monsters with no special case: zero against any multi-hit intent, and zero against a Snapping Jaxfruit, a Sludge Spinner, a Vine Shambler or a Fogmog either. `FlameBarrierPower.AfterDamageReceived` is a hook on the DAMAGE, so it belongs where Thorns already was — inside the per-hit helper. Three details came off `CreatureCmd`, which is the only place the hook is raised: it fires **per DamageResult**, it fires **whether or not block absorbed the hit** (the neighbouring `AfterCurrentHpChanged` is guarded on `UnblockedDamage > 0` and this one is pointedly not), and it is **skipped when the blow killed its target** (`!WasTargetKilled \|\| !IsDead`) — so a player who dies to the hit does not retaliate, and one a relic revives does. |
| E28 | Punch Construct's FAST_PUNCH applied no Frail | source read, no capture | **A rider behind the multi-hit break.** FAST_PUNCH is `MultiAttackIntent(FastPunchDamage, FastPunchRepeat)` plus a `DebuffIntent`, and the emulator's intent declares `Hits: 2` — so it took the multi-hit path, which `break`s before every per-monster rider. The construct's signature move dealt the right damage and left the player undebuffed, which reads downstream as blocking too much on the turn after. The break is gone: the generic path takes `Math.Max(1, Hits)` and the riders belong to the attack as a whole. Audited rather than assumed — of the 24 riders past it, this was the only one whose move was multi-hit; KinPriest, Lagavulin Matriarch, Vantom and Fossil Stalker all have theirs on single-hit moves. |
| E29 | `player.gold` step 27: 114 live, 108 here — the first combat reward of the run | `1UL0BRX8WC` | **Phial Holster's potions rolled off the Rewards stream.** `PhialHolster.AfterObtained` is `GainMaxPotionCount(1)` and then `CreateRandomPotionsOutOfCombat(2, Rng.CombatPotionGeneration)`; the emulator drew both from `PlayerRng.Rewards`, which is the stream every card reward, shop and transformation in the run also reads. Two draws the game never makes there put everything downstream at the wrong position. Its `+1` potion slot was unmodelled too, so the owner could carry two where the game gives three — `max_potion_slots` in the capture reads 3 for this run and 2 for the other nine, which is also what settles the BASE: `Player.initialMaxPotionSlotCount` is 3, but at A8 every capture says 2, so the decompiled constant is the un-ascended one. |
| E30 | (same seed) gold still 106 after E29 | `1UL0BRX8WC` | **A four-draw fudge on top.** `AdvanceRewardRngForNeowRelic` burned 4 Rewards calls for Phial Holster "to keep the stream aligned" — the same shape as the 18 Kaleidoscope used to burn. With the potions moved to their real stream the fudge was all that was left, and it put the first combat's gold at stream position 5 where the game had it at 1. Removed. **The other two rows stayed as a standing debt** — Hefty Tablet and Lead Paperweight really do draw from Rewards (`CardFactory.CreateForReward`) while the emulator rolled their cards off `Rng.UpFront`, so their counts stood in for real draws with nothing to check them against. Both are now captured and modelled, and the table is empty: see E43. |
| E31 | `player.energy` step 97: 2 live, 1 here | `1UL0BRX8WC` | **The opening hand skipped Slither's cost roll.** `Slither.AfterCardDrawn` re-rolls its card's cost off `Rng.CombatEnergyCosts` on every draw INTO HAND, and the opening hand is a draw — but `CombatFactory` deals it with a bare `Hand.Add(DrawPile[0])`, so an enchanted card in it kept its printed cost. A Wood Carvings run enchanted a Bash on floor 8 and opened the floor-9 fight paying 1 for it; the emulator paid 2. Worth knowing why it hid: the replay compares the deck as `(id, upgraded)` pairs, which an enchantment does not change, so an unapplied enchantment is invisible there and surfaces as energy three steps later. |
| E32 | (same fix) `combat_energy_costs` restarted every fight | `1UL0BRX8WC` | **The one named stream that was neither fast-forwarded nor written back.** Every other stream is built by advancing a fresh `CountingRandom` to the run stream's `CallCount` and then advanced back at combat end; `CombatEnergyCosts` did neither, so each combat re-read it from position 0 and two Slither draws in two fights returned the same value where the game returns consecutive ones. Found by inspection while chasing E31 rather than by the capture — the capture's only Slither draw is at position 0, where both models agree. |
| E33 | `state_type` step 90: game `monster`, emulator `rewards` — the emulator wins a fight the game is still fighting | `25TS4F5T37` | **A Fogmog's Eye With Teeth cannot be killed, and the emulator killed it.** `IllusionPower` is three rules the emulator had none of: `ShouldCreatureBeRemovedFromCombatAfterDeath` is false, so the eye stays in the roster; `AfterDeath` forces a REVIVE_MOVE with `MustPerformOnceBeforeTransitioning`, so its next turn is spent healing to full rather than acting; and `ShouldPowerBeRemovedOnDeath` keeps its buffs — IllusionPower included — through the death, so it does it again, forever. What made the damage compound is target resolution: with the eye dead in the emulator and alive in the game, every blow the live run spent on the illusion resolved against the emulator's first LIVING enemy, which was the Fogmog. It died floors early. |
| E34 | (same fix) a fight could end on the wrong body count | `25TS4F5T37` | **The emulator had no notion of a secondary enemy.** `Creature.IsPrimaryEnemy` says it outright — "a secondary enemy will automatically die unless there's also a living primary enemy" — and carrying `MinionPower` or `IllusionPower` is what makes one. Every all-dead check counted the whole roster instead, which is wrong in both directions at once: a Fogmog's eye revives forever, so a fight it outlived could never be won; and a Gas Bomb left standing after its Living Fog died would hold a finished fight open. |
| E35 | (same fix) ILLUSION re-summoned and the re-summon ate the revive | `25TS4F5T37` | **A guard standing in for the missing mechanic.** The emulator's ILLUSION branch ran only when no eye was alive, sweeping away any dead one first — which is a fair approximation of "the eye comes back" and becomes actively wrong once the eye really does. It deleted an eye in the middle of reviving and inserted a fresh one at the front of the roster, moving the creature a target index names. `ILLUSION_MOVE` is the move machine's INITIAL state with nothing leading back to it: it fires once per combat and never again. The same shape as E30 — a compensation that outlived what it compensated for. |
| E36 | `battle.enemies` step 78: block 8 live, 16 here | `SAM9XS24LM` | **A Sewer Clam gained its Plating block twice.** `PRESSURIZE_MOVE` is `PowerCmd.Apply<StrengthPower>(4)` and nothing else — the block the emulator also gave it on that turn was invented. The clam's block comes from `PlatingPower` alone, which grants it to every owner at the end of its side's turn, so the buff turn paid out twice. |
| E37 | (same seed) block 9 live, 8 here on the first turn, then 8 live and 9 here | `SAM9XS24LM` | **Plating decayed a turn early, and in the wrong order.** `PlatingPower` decrements on `AfterSideTurnStart` and grants its block on `BeforeSideTurnEndEarly`, so the block a turn ends with is the ALREADY decremented amount — and `AfterSideTurnStart` skips the decrement entirely for enemies on round one. The emulator granted first and decremented after, which is a point of block ahead of the game for the whole fight. Watch the counter: `CombatState.Turn` counts from ZERO, so the first enemy phase is Turn 0 and a `> 1` guard puts the decay a turn late — which is how the first attempt at this fix traded one off-by-one for another. |
| E38 | `player.hp` step 126: 9 live, 8 here, after 126 clean steps | `NXV45HW43K` | **The Chosen Cheese did nothing.** `ChosenCheese.AfterCombatEnd` is `GainMaxHp(1)`, and gaining a maximum heals by the same amount — so a fight won at 2 HP ends at 3 before Burning Blood's six. The emulator could already be GIVEN the relic by Room Full of Cheese and then ignored it. One point per combat, compounding, and invisible until the arithmetic happened to be checked at a step where nothing else was wrong. |
| E39 | `player.block` step 122: 3 live, 5 here | `25TS4F5T37`, `XTLVVPKFBF` | **A Flyconid's FRAIL_SPORES applied no Frail.** `FRAIL_SPORES_MOVE` is `SingleAttackIntent(SporeDamage)` then `DebuffIntent`, so the readout calls it an Attack — and the emulator's `PowerCmd.Apply<FrailPower>(2)` sat in the debuff branch, keyed on the intent's magnitude, where an Attack intent never arrives. The damage landed and the Frail did not, so every Defend after it blocked five where the game blocked three. **Third time for this exact shape** after E25 and E28; the emulator's own comment on the intent even said "announced as an attack". |
| E40 | `player.deck` step 82: the game upgraded Whirlwind, the emulator Shrug It Off | `L9R346P3YD` | **Doors of Light and Dark's Light door rolled off the wrong stream, and sorted on the wrong key.** `Light()` is `Deck.Where(IsUpgradable).StableShuffle(base.Rng).Take(Cards)`: the EVENT's own Rng, not `Rng.Niche` (E14 again), and `StableShuffle` sorts by ModelId — the slugified class name, compared ordinally — where the emulator sorted by its own numeric ids. Either one alone puts a different card under the same draw. |
| E41 | `player.hand` step 87: seven cards and four energy live, five and three here | `L9R346P3YD` | **A range over an enum stopped being true.** `IsEliteEncounter` was `>= BygoneEffigy and <= WaterfallGiant`, which was correct when WaterfallGiant was the last name declared and silently wrong once `Architect` and `SkulkingColony` were appended after it — so a Skulking Colony elite did not read as one and Booming Conch never fired. The same range also swept in every boss, which the game excludes: BoomingConch asks for `RoomType.Elite`, and a boss room is `RoomType.Boss`. The six act-1 elites are named now. |
| E42 | `state_type` step 1: game `rewards`, emulator `event` (was **O8**) | `J09SPL8Y3V` | **Neow's Bones was four things at once.** `AfterObtained` shuffles `GetValidRelics` on `PlayerRng.Rewards` and takes two, offers them as a `RewardsSet(...).WithSkippingDisallowed()` the player answers twice, and adds its curse only after. The emulator took two independent `Rng.UpFront.NextItem` draws and applied them on the spot: the wrong stream, a draw that can repeat itself where a shuffle-and-take cannot, no screen, and a candidate list of only the positives rather than all 26 valid Neow relics in `AllPossibleOptions` declaration order — which is load-bearing, because the shuffle is over that exact sequence. Two more fell out of building the screen: a relic claimed from a reward screen is obtained through `RelicCmd.Obtain` and so **runs its pickup effect** (the capture's Silken Tress zeroes the run's gold the moment it is taken), and the claim that EMPTIES the screen returns to Neow by itself, with no separate action, the way the card-reward path already did. |
| E43 | `state_type` step 1: game `card_select`, emulator `event` | `25TS4F5T37` (Lead Paperweight), `XTLVVPKFBF` (Hefty Tablet) | **Both of E30's stand-in draw counts are gone, replaced by the screens they were standing in for.** Each relic offers cards on a `CardSelectCmd.FromChooseACardScreen` grid and the emulator granted one card off `Rng.UpFront` instead: Lead Paperweight offers TWO from the Colourless pool at `CardCreationSource.Other`/RegularEncounter odds (rarity, card, upgrade — three draws each, so **six**, exactly its old fudge), Hefty Tablet THREE from the owner's pool filtered to Rare at Uniform odds with `NoUpgradeRoll` (one draw each, so **three**, exactly its old fudge). That the counts matched is why nothing downstream moved when they were replaced — the fudges were right about the arithmetic and wrong about everything else. Hefty Tablet's Injury also arrives WITH the card the grid hands over, not before it: `CardPileCmd.Add` takes one list holding the curse with the chosen card inserted at its front, and the capture shows both land in the same snapshot. |
| E44 | `state_type` step 1: game `card_select`, emulator `event` | `J09SPL8Y3V-a8-precisescissors`, `SAM9XS24LM-a8-precariousshears` | **Precise Scissors and Precarious Shears chose the card for the player.** Both are `CardSelectCmd.FromDeckForRemoval` — CardsVar(1) and CardsVar(2), the second followed by DamageVar(16) — and the emulator called `RemoveLowestPriorityCard`, whose priority list starts with the curse placeholder: **Ascender's Bane**, which `IsRemovable` excludes outright, so it removed a card the game will not even offer. `BeginDeckSelection` already models this (Luminous Choir's comment says why: "the emulator choosing for them, which is the whole cost of the option"), including the count and the follow-up HP loss. Precarious Shears also took its 16 damage BEFORE the removal rather than after. **Four other sites still call `RemoveLowestPriorityCard`** and are the same defect waiting for a capture. |
| E45 | (same fix) a Neow selection left a flag set for the rest of the run | `J09SPL8Y3V-a8-precisescissors` | A selection Neow opens must return to Neow, which stays up for one more Proceed — and the deck-selection completion path did not, so `NeowAwaitingProceed` stayed set forever. That is worse than landing on the wrong screen once: the flag is read again by the NEXT card reward the run claims, so a combat's reward screen went back to the ancient two floors later. Caught because the divergence moved from step 1 to step 25 rather than disappearing. |
| E46 | `player.gold` step 111: 121 live, 101 here (was **O9**) | `J09SPL8Y3V-a8-precisescissors` | **The emulator robbed a corpse.** `ThieveryPower.Steal` opens `if (Target != null && !Target.IsDead && Target.Player.Gold > 0)`, and a Gremlin Merc's move attacks BEFORE it steals — so the blow that kills the player takes no gold with it. The emulator's steal checked only that the player had gold. Worth noting how this presented: twenty gold, in the last snapshot of the run, a hundred clean steps in, with every field before it agreeing. It reads like drift and it was a missing clause. |
| E47 | `player.deck` step 62: the game upgraded two Strikes, the emulator a Strike and a Defend | `NXV45HW43K-a8-cursedpearl` | **`IsUpgradable` was a hand-kept list of fourteen card ids against the thirty-seven the game actually declares.** `CardModel.IsUpgradable` is `CurrentUpgradeLevel < MaxUpgradeLevel`, and the cards that override `MaxUpgradeLevel` are every curse and status; the emulator's list held fourteen of them. The twenty-three it missed were eligible for every upgrade in the game, which is invisible while an upgrade is CHOSEN and decisive the moment one is RANDOM — Doors of Light and Dark shuffles the upgradable cards and takes two, so Greed sitting in a Cursed Pearl run's candidate list made it fourteen names instead of thirteen, a different shuffle, and a different pick. `MaxUpgradeLevel => 0` is extracted into `Cards.g.cs` now and the list is gone. |
| E48 | `player.deck` step 27: the game offered Sword Boomerang+, the emulator Body Slam+ | `RRRR6WR3C4-a8-silvercrucible` | **`||` short-circuited a draw the game always makes.** `CardFactory.CreateForReward` calls `RollForUpgrade` for every reward card unless `NoUpgradeRoll` is set, and `RollForUpgrade` draws its float on its FIRST line, before it asks whether the card is upgradable. The emulator wrote the answer as `silverCrucibleUpgrade || RollCardUpgrade(...) || UpgradedByEggs(...)`, so a run holding Silver Crucible — whose upgrade is `TryModifyCardRewardOptionsLate`, applied after the rolls and changing nothing about them — spent two rewards-stream values per card where the game spends three. Every card the stream produced from the second one on was somebody else's. |
| E49 | `state_type` step 1: game `card_select`, emulator `event` | `RRRR6WR3C4-a8-pomander` | **Pomander picked the card for you.** `AfterObtained` is `CardSelectCmd.FromDeckForUpgrade` at CardsVar(1); the emulator called `UpgradeFirstCard`, which takes the deck's first upgradable card — a Strike, in every starting deck. Same shape as E44, different blessing. |
| E50 | `state_type` step 3: game `event`, emulator `card_select` | `N11HWGCNUN-a8-newleaf` | **New Leaf answered its screen a step late.** It is `FromDeckForTransformation` then `TransformToRandom` on `Rng.Niche`, and it was still riding the older `TransformSelectedDeckIndex` path that predates `BeginDeckSelection` — so the game was back at the event with the card already transformed while the emulator still held the selector open. Converting it also removes one of the three remaining users of that path. |
| E51 | `player.relics` step 1: game holds two, emulator three | `P14DQ9GNPW-a8-smallcapsule` | **Small Capsule granted its relic instead of offering it.** `AfterObtained` is `RewardsCmd.OfferCustom` with a single `RelicReward` — a SCREEN. Granting it outright also ran the relic's own pickup effect a step early, which is the same pair of consequences E42 found in Neow's Bones; the machinery that fix built (`PendingBonusRelicRewards`) is what this now uses. |
| E52 | (no capture) Stone Humidifier did nothing at all | read from `StoneHumidifier.cs` | `AfterRestSiteHeal` grants `MaxHpVar(5)`, and the relic had a constant in `RunConstants` and no implementation anywhere. It ignores the hook's `isMimicked` flag, so the event that heals you like a rest pays it too. |
| E53 | `state_type` step 113: game `event`, emulator `map` | `RRRR6WR3C4-a8-pomander` | **An event that hands out rewards gets its result page back, and the SKIP path walked past that.** Every one of the four such events awaits `RewardsCmd.OfferCustom` and calls `SetEventFinished` on the line below, so the run spends one more Proceed than the emulator did. Neow already had this return; the reason nobody noticed the gap is that Neow's rewards are `WithSkippingDisallowed`, so the skip branch — which returned straight to the map from underneath both checks — was only ever reachable from an event. Whispering Hollow offers TWO potions and a run with a full belt declines the second, which is exactly the path that exposed it. Both exits share one `LeaveRewardScreen` now. |
| E54 | (no capture) both options of two events belonged to other events | read from `FieldOfManSizedHoles.cs`, `SpiritGrafter.cs` | Found by chasing E44's closing note that four sites still called `RemoveLowestPriorityCard`. The removal was the least of it. **Field of Man-Sized Holes**: Resist is `FromDeckForRemoval` at CardsVar(**2**) followed by `AddCursesToDeck(Normality)` — two cards the player picks and a curse for them, against one card of the emulator's choosing and no curse. **Spirit Grafter**: LetItIn heals 25 and adds a Metamorphosis; Rejection is `FromDeckForUpgrade` and then 10 unblockable damage. The emulator removed a card and granted 3 max HP for the first, and transformed-then-upgraded the deck's first card for the second. Neither event's numbers appear anywhere in its own source. **Two sites still call `RemoveLowestPriorityCard`** — the shop's removal service and Empty Cage — and both need a return-phase concept the deck selection does not have yet, since its completion path always lands on `RunPhase.Event`. |
| E55 | `player.gold` step 90: 151 live, 142 here | `9V9WN98106-a8-neowstalisman` | **The Gremlin Merc's fight paid no gold, because a flat 0 stood in for a rule.** `GremlinMercNormal.CalculateGoldProportion` pays in FULL when nothing escaped, half when a Fat Gremlin escaped having stolen nothing, and nothing when one escaped with the loot. The flat 0 was fitted to three traces that had all captured the escape, and this capture — which kills the gremlin — is paid 9 in full. **The first fix was worse than the bug**: reading "nothing escapes in the emulator" off the absence of an escape FLAG, when the escape is modelled by zeroing the gremlin's HP, broke all three of those traces at once. A Fat Gremlin that leaves and one that dies were indistinguishable, and they owe the player opposite things, so `EnemyState.Escaped` and `CombatState.FatGremlinEscaped`/`MercGoldWasStolen` now say which happened. Missing the gold was the visible half; missing the DRAW was the expensive one, and changed the card reward four steps later. A zero proportion really does skip the draw — `RewardsSet` guards the row behind `if (GoldProportion > 0f)` — which is why the flat 0 passed for as long as it did. |
| E56 | (same trace) `state_type` step 91: game `rewards`, emulator `card_reward` | `9V9WN98106-a8-neowstalisman` | **Stolen gold came back mid-combat instead of on the screen.** `HeistPower.BeforeDeath` calls `combatRoom.AddExtraReward(new GoldReward(Amount, wasGoldStolenBack: true))` — the player CLAIMS it, and the capture shows it as its own row, "80 Gold (stolen back)", beside the fight's ordinary 9. The emulator added it straight to the run's gold, and only outside the merc's own encounter, which is the one fight the power exists for. |
| E57 | `state_type` step 1: game `bundle_select`, emulator `event` | `ZY1E5128P6-a8-scrollboxes` | **Scroll Boxes needed a screen the emulator did not have.** `GenerateRandomBundles` draws six cards off `PlayerRng.Rewards` — two Commons and an Uncommon per bundle, all six distinct because the used set spans both — and `FromChooseABundleScreen` offers the two bundles, of which the player takes one WHOLE. The emulator granted three cards rolled off `Rng.UpFront`. This is the first screen modelled from scratch rather than reused: `RunPhase.BundleSelect`, answered in two actions the way the game's is (`select_bundle`, then `confirm_bundle_selection`), state list 18 so an agent can see all six cards, and the Neow return every other blessing already had. The generation reproduced the capture's two bundles exactly on first contact, which is the part that would have been expensive to get wrong. |
| E58 | `player.deck` step 87: the game offered Anger, the emulator One Two Punch | `buff-smoke` (buffed capture) | **An elite rolled its relic before its three cards.** `RewardsSet` builds gold, potion, card, relic and populates them in that order, sorting by `RewardsSetIndex` only afterwards — so the order the SCREEN shows them in (relic above the card) is not the order they were rolled in. `PullRelic` takes its rarity off `PlayerRng.Rewards` like everything else, so rolling it early handed the card offer the relic's value and shifted all three cards by a draw; the offers disagreed on all three RARITIES, which is the signature of a misaligned stream rather than wrong odds. **Thirty traces missed this because not one had ever claimed a combat relic reward** — the three relic claims in the whole set are Neow screens on step 2. It took a buffed run surviving to a floor-14 elite to reach the screen. |
| E59 | `player.energy` step 7: 2 live, 1 here | `8QKMNR4T2W-a8-buff200` | **Gremlin Horn was not modelled at all** — no reference to it anywhere. `AfterDeath` gives EnergyVar(1) and CardsVar(1) for anything that dies on the other side, so the capture's second Strike of the run was free and drew a card. It checks only the SIDE, not the `wasRemovalPrevented` flag it is handed, so it pays out even for a death something else undoes — which is why it sits above the revive branches in `HandleEnemyDeaths` rather than after them. Held from floor one via Large Capsule, and caught at step 7: nothing about this needed depth, only a run that happened to HOLD it. |
| E60 | `player.gold` step 322: 312 live, 337 here | `3PFLW9XC5D-a8-buff400` | **A boss paid 100 gold where the game pays 75.** Its Min and Max are both 100 and A8's Poverty multiplier takes a quarter off BEFORE the roll — the same 0.75 already baked into the monster range (10-20 becomes 7-15) and the elite one (35-45 becomes 26-33). Only the boss branch was written as a flat 100, and no capture could contradict it until one finally won an act. |
| E61 | `player.deck` step 323: the emulator upgraded two attacks the game did not | `3PFLW9XC5D-a8-buff400` | **A boss handed over a relic it does not give.** `RewardsSet`'s Boss case is gold, potion, card and nothing else; the RelicReward in the Elite case directly above it has no counterpart there. The emulator set `PendingRelicReward` for both, so beating act 1 granted a Whetstone — whose pickup upgrades two attacks, which is how a wrong relic list showed up as a wrong DECK one step later. |
| E62 | `battle.enemies` step 55: the game's Strangler has 5 block, the emulator none | `7WGQ2VNJ4M-a8-buff400` | **A move's second half was never modelled.** `SlitheringStrangler.ThwackMove` attacks and then `GainBlock(5)` — the move carries a DefendIntent beside its attack, which the emulator's own intent table already noted in a comment and nothing ever acted on. Lash is the same enemy's other attack and gains nothing, so the announced damage is what tells them apart at execution time. |
| E63 | `state_type` step 9: game `event`, emulator `monster` | `act2-jump` (act 2 capture) | **An act's map opens on its Ancient, and the emulator had no such node.** `StandardActMap` sets `StartingMapPoint.PointType = Ancient` in EVERY act, after every other assignment so it wins — and `RunManager.EnterAct` only puts the run ON that point for act 1 with Neow; every act after opens a `MapRoom` with nothing entered, so the ancient is the one thing to travel to. The emulator arrived already standing on the start node and offered row one, which was a monster. Three parts: `NodeAncient` as a type, `AwaitingActStartNode` for "looking at the map without having stepped onto it", and the stamp moved to the END of map generation — set at the top, the type-assignment pass overwrote it and the node came out Unassigned, which is why the first attempt produced an empty option list. |
| E64 | `player.hp` step 9: 276 live, 264 here | `act2-jump` | **Entering an ancient heals you, and nothing knew that.** `AncientEventModel.BeforeEventStarted` heals `MaxHp - CurrentHp`, and A8's WearyTraveler pays 80% of it: the capture crosses into act 2 on 264/280 and stands on Pael at 276. **This is also where the run's opening HP comes from** — Neow calls `SetCurrentHpInternal(0)` first, so its heal is 80% of the whole 80 max, which is the 64 the emulator has always hardcoded in `Reset` without knowing why. |
| E65 | (no capture yet) each act's ancient was a discarded draw | read from `RunManager.GenerateRooms`, `ActModel.GenerateRooms` | **The run now knows which ancient each act opens on.** `_rooms.Ancient = rng.NextItem(GetUnlockedAncients().Concat(sharedAncientSubset))` is the last line of an act's generation, and the emulator spent that draw on a bare `NextDouble` and threw the answer away. Both act-1 regions declare Neow alone, so act 1 is a one-item pick that still costs its draw; Hive declares Orobas, Pael and Tezcatara, Glory declares three of its own. **The shared pool was the part that was wrong**: `SharedAncientPrefixDraws = 2` had been read as "the pool is empty", when a ONE-item pool shuffles in zero draws and gives exactly the same count. `ModelDb`'s own comment settles it — "we only have 1 right now. That's Darv." A capture (`3PFLW9XC5D`) opens act 2 on DARV, which is in no act's own list, and reproducing that is what proved it. The subsets are dealt for real now: `NextInt(remaining + 1)` per act after the first, taken off the front and removed from what is left. |
| E66 | `ACT2TEST01` opens act 2 on Pael, the emulator drew Tezcatara (was **O14**) | seven act-2 captures | **Hive's encounters carry tags and the emulator's tag table only knew act 1's.** The consequence is bigger than a wrong encounter order, because `GrabBag.GrabIndex` REJECTION-SAMPLES: it redraws until the tag predicate is satisfied, so an encounter the game tags and the emulator does not changes how many draws a grab COSTS. Every draw after it — the boss, the ancient, the next act's whole generation — lands somewhere else. The ancient was reading the right list at the wrong stream position, which is why no list size or ordering could explain it. Bowlbugs weak and normal are both Workers, as is the Slumbering Beetle; Exoskeletons weak and normal share theirs; Chompers, Tunneler and the Thieving Hopper each have one. Five of seven captures agreed before, seven of seven after. |
| E67 | `player.deck` step 10: taking Pael's Horn adds two Relax; the emulator added nothing | `act2-jump` | **Six of the ancients' blessings do something when taken, and none of them did.** Pael's Horn adds two Relax (the one a capture pins), Storybook a Brightest Flame; Sand Castle upgrades CardsVar(6) cards StableShuffled on **Rng.Niche**, which is neither the event stream nor UpFront; Alchemical Coffer gains four potion slots FIRST and then rolls four potions off **CombatPotionGeneration**, the same shape and the same stream-choice trap as Phial Holster; Yummy Cookie and Biiig Hug hand the player a four-card selection rather than choosing. The Light Door's shuffle-and-take is now shared as `UpgradeRandomCards`, since Sand Castle is the same operation on a different stream and count. |
| E68 | (no capture yet) two more ancient blessings, and a screen the emulator could not express | read from `GlassEye.cs`, `PaelsTooth.cs` | **Glass Eye** is FIVE card rewards on one screen — Common, Common, Uncommon, Uncommon, Rare — each offering three of that rarity, claimed one after another. Uniform odds with `NoRarityModification` means no rarity roll, so each card is a pick and an upgrade roll: two draws, thirty in all, off `PlayerRng.Rewards`. The queue machinery already existed for the Crystal Sphere (`PendingCardOffers`, `GenerateFixedRarityCardOffer`); what was missing was a way for a RELIC pickup to hand back a queue, which is `RunFollowUp.BonusCardOffers`. **Pael's Tooth** removes five cards the player picks, and `FromDeckForRemoval` takes a FILTER it passes `c.IsUpgradable` to — a different screen from a plain removal, not a different answer to the same one, so it is `DeckSelection.RemoveUpgradable` rather than a flag on the existing kind. A deck of nothing but curses offers it nothing where a plain removal would offer everything. |
| E69 | (no capture yet) Sea Glass, and the draw Orobas was throwing away | read from `SeaGlass.cs`, `Orobas.cs` | **CardsVar(15) / 3 = five cards of each rarity from ANOTHER character's pool**, all fifteen on one grid — thirty draws, a pick and an upgrade roll each, the same budget as Glass Eye. Which character is decided by the FIRST draw Orobas spends, over `Characters.Where(c => c.Id != Owner)`; `ModelDb.AllCharacters` is Ironclad, Silent, Regent, Necrobinder, Defect, so for an Ironclad run that is four in that order. The emulator was making that draw and discarding the answer, which was fine while nothing read it and wrong the moment Sea Glass could be taken. All five character pools were already extracted, so the pool itself cost nothing. |
| E70 | (no capture yet) the last four ancient blessings, and four enchantments that did not exist | read from the enchantment and relic models | **Imbued, Clone, Goopy and Tezcatara's Ember** are in `Enchantment` now, appended so no existing value shifts — the observation carries that number. Their `CanEnchant` rules join the others: Imbued takes skills, Goopy takes Defend-tagged cards (the same Basic-name stand-in `Spiral` already uses, since card tags are not extracted). **Pael's Claw** goops every card that will take it and **Nutritious Soup** embers every Basic Strike, both with no screen at all; **Electric Shrymp** and **Pael's Growth** ask the player, and Growth applies Clone at FOUR where every other event enchantment applies at 1 — which is why `BeginDeckSelection` now takes an amount instead of inferring one. Ember's combat half is here too: `OnEnchant` zeroes the printed cost for good (not a free-this-turn flag) and it adds its amount to a powered attack, every play, like Sharp. |
| E71 | (no capture yet) three of the four new enchantments did nothing in a fight | read from the enchantment models | **Goopy** exhausts its card (`OnEnchant` adds the keyword), adds `Amount - 1` block — so a freshly goopied card adds NOTHING and only starts paying once played — and `AfterCardPlayed` bumps the amount, so it is worth one more block every time. **Imbued** sinks to the bottom of the draw pile; the turn-1 reorder had been written against `ShouldStartAtBottomOfDrawPile` and stubbed to `false` for want of an enchantment that had it, so this was a one-line fill of a hole left deliberately. **Clone** turned out to be right already: it is an EMPTY `EnchantmentModel` that overrides nothing, and its effect is at a REST SITE — `CloneRestSiteOption` copies every Clone-enchanted card into the deck — so doing nothing in a fight is correct, and there is now a test saying so on purpose rather than by omission. |
| E72 | (no capture yet) the two halves of the enchantments that outlive a single action | read from `Imbued.cs`, `Goopy.cs`, `CloneRestSiteOption.cs` | **Imbued auto-plays on turn 1**, from the BOTTOM of the draw pile where the reorder puts it. The emulator's `AutoPlayQueue` was only drained AFTER an action, so there was no moment for something queued at combat START to fire; `Step` drains it first now, before the player's move, which is where the game's pre-play phase sits. The card is taken OUT of the draw pile as it is queued, because `AutoPlayCore` files it into discard or exhaust when it is done and would otherwise duplicate it. **Goopy's growth now reaches the run deck** — the game bumps `Card.DeckVersion.Enchantment.Amount`, so the block is permanent — matched by card identity at combat end, since the emulator has no link from a combat card back to the deck card it came from. **Clone's rest option** is Pael's Growth's `TryModifyRestSiteOptions`, offered whenever the RELIC is held rather than when the deck holds a Clone card; with none it copies nothing. That is the whole of what Clone does, so a run that takes Pael's Growth and never rests gets nothing from it. |
| E73 | (no capture yet) an Imbued card drawn into the opening hand never played | found by asking what happens with a small deck | The turn-1 reorder MOVES an Imbued card to the bottom of the draw pile; it does not stop the opening draw reaching it. **A deck of five or fewer draws its own bottom card**, so the Imbued one lands in hand — and the game plays it from there, leaving the turn to start on FOUR. `QueueCombatStartAutoPlays` scanned only the draw pile, so in that case it queued nothing: the card never played at all and the player kept a card the game had spent. It scans the hand too now. Worth noting the shape of the bug — the enchantment worked in every ordinary deck and failed only where the pile ran out, which no capture would have reached, because a run's deck only shrinks below six through deliberate removal. |
| E74 | (no capture yet) the merchant's removal service and Empty Cage both chose which card went | found by converting the last `RemoveLowestPriorityCard` call sites | Both opened NO screen: they walked a preference order the emulator invented — Ascender's Bane, then Defend, then Strike, then whatever sat last — and Ascender's Bane is a card the real removal screen will not even offer. Both are `FromDeckForRemoval`, so the pick is the player's and is the whole of what the gold (or the relic) buys. The blocker was structural: a selection's completion path always landed on `RunPhase.Event`, which for a shop or a relic pickup is a screen that is not there. `SelectionReturn` now records where the selection was opened FROM — the same fix E53 made for the rewards screen — so a shop selection goes back to the shop and a relic's goes to the map. `RemoveLowestPriorityCard` is deleted, not just unused. |
| E75 | (no capture yet) the merchant restocked its removal service after every purchase | read from `MerchantCardRemovalEntry` | `IsStocked => !Used`: the service is stocked **once per visit**, and gold was the only limit the emulator modelled. `ShopRemovalsUsed` is the RUN's total and sets the price (`BaseCost + PriceIncrease * CardShopRemovalsUsed`, 100 + 50 at A8); the per-visit flag is a separate thing that `EnterShop` clears. Also reordered: the screen opens BEFORE the gold is taken, matching `DoLocalMerchantCardRemoval`, so a removal that cannot open is not a sale. |
| E76 | (no capture yet) Zen Weaver's three options matched none of its three options | found by reading the model behind an `UpgradeFirstCard` call site | The emulator offered heal 12 / upgrade a card / +5 max HP. The event **sells removals**: Breathing Techniques puts two Enlightenments in the deck for 50, Emotional Awareness removes one for 125, Arachnid Acupuncture removes two for 250 — and the last two are `CreateLockedOption` below their price, shown but dead. `IsAllowed` was wrong too: it is `Players.All(p => p.Gold >= 125)`, not upgradable-or-hurt, so the weaver was appearing for runs that could not use it and being offered options it does not have. |
| E77 | (no capture yet) Reflections' two options were a transform and an upgrade | read from `Reflections.cs` | Neither option asks the player anything. **Touch a Mirror** downgrades up to two upgraded cards off the event's own Rng and then upgrades up to four upgradable ones — the second list derived AFTER the downgrades, so the two just knocked down can come straight back up. **Shatter** copies the ENTIRE deck (`CloneCard`, so upgrades and enchantments ride along, and the loop bounds itself on the pre-copy size) and then adds a Bad Luck. Its `Deck.Count > 0` gate came off too: the model declares no `IsAllowed` at all. |
| E78 | (no capture yet) Tinker Time offered three options on a page that has one | read from `TinkerTime.cs` | The event builds a card over **three pages**: one option that turns the page, then two of Attack/Skill/Power, then two of the three riders belonging to the chosen type, ending in a Mad Science carrying both choices. Each pair is `TakeRandom(2, Rng)` = `UnstableShuffle(rng).Take(2)`, which permutes all three — taking only the draws the two shown need would read the next page's values one early. The mask was wrong for a second reason: Tinker Time builds its options into a `List`, so `EventOptions.g.cs` has no count for it and the fallback offered a flat 0..3. **Still open:** the Mad Science card's combat behaviour. Its type and rider are recorded on the `CardInstance`, but playing it runs the generic 12-damage attack from the card table — the nine riders need `CuriousPower` and `ImprovementPower`, neither of which exists yet. |
| E81 | (no capture yet) four encounters had lost their tags, three of them Glory's | read from every `EncounterModel.Tags` | The tag table was hand-transcribed after E66 and had drifted: **KnightsElite**, **both halves of ScrollsOfBiting**, and **TunnelerNormal** — which carries `Burrower, Chomper` where the weak version carries only `Burrower`. A missing tag is worse than a wrong one, for the reason E66 established: `GrabIndex` rejection-samples, so it changes how many draws a grab COSTS and moves the boss, the ancient and the whole next act. Three of the four are act 3's, which was set up to be handed exactly what E66 did to act 2. The table is GENERATED now (`scripts/generate_encounter_tags.py`), and a tagged model it cannot map to an `ActOneEncounter` is an error rather than a skip — silently dropping one is the failure the file exists to prevent. |
| E82 | (no capture yet) act 3 generated against Hive's encounters | read from `Glory.GenerateAllEncounters` | **Glory's four pools were a placeholder returning Hive's**, so every act-3 room, elite and boss was an act-2 one. The same generator now emits all sixteen pools from each act's own `GenerateAllEncounters()`, filtered by `RoomType` plus the `IsWeak` override, in declaration order — which is what the grab bags are dealt in, and is NOT the act's `BossDiscoveryOrder`. The argument that Glory's four are right is that the generator reproduces act 1's and Hive's twelve EXACTLY, all of which have been checked against live captures; `EncounterTagsAndPoolsTests` pins that, so the generated table and the constants it was derived against cannot drift apart in silence. Act 1 and Hive still read their verified constants — only Glory switched — because there is no reason to move a pool a capture has already agreed with. |
| E83 | (no capture yet) eight Hive monsters announced their A9 damage at A8 | audited from every monster's `GetValueIfAscension` pairs | E11's class, in the one place that was never swept. `CombatFactory`'s opening intents were converted to `Ascension.Value` years ago, but `ChooseIntents` OVERWRITES every opening intent right after the roster is built — so `EnemyAI.SelectIntent` is what an agent actually reads, and it was still transcribing the Deadly branch as a bare literal. Fixed for the Hive monsters, which is what an act-2 run meets: Exoskeleton, both Bowlbug forms, Myte, Louse Progenitor, Ovicopter and Hunter-Killer. **Louse Progenitor's Curl is the counter-example worth keeping**: 18 is `GetValueIfAscension(ToughEnemies, 18, 14)` and Tough IS live at A8, so a sweep that "fixed" every literal next to it would have broken it, and there is a test saying so. `scripts/audit_ascension_literals.py` is the worklist for the 66 that remain — it flags a bare high value anywhere in a monster's case block, so it is a place to look rather than a verdict. |
| E84 | (same batch) three of the eight were wrong a second way | read from the move state machines | **Exoskeleton's SKITTER** is `MultiAttackIntent(SkitterDamage, SkitterRepeats)` where the damage is a flat 1 and the REPEATS are what ascension moves — so the emulator's flat 4 was the A9 hit count read as a damage number. **Hunter-Killer's PUNCTURE** announced 24, which is `8 x 3` folded: E10's shape, and it matches only while the creature has no Strength, because the game adds Strength to EACH hit. **The Ovicopter's TENDERIZER** declares `SingleAttackIntent` and THEN `DebuffIntent`, and the readout follows the declaration — so it is an Attack whose number is damage, not a Debuff (E12). Retyping it moved its damage into the attack branch, which would have dropped its Vulnerable on the floor; the rider moved with it, and a test holds that. |
| E85 | (no capture yet) two encounters in a pool the emulator could not build at all | swept every act's pools for build failures | **`ExoskeletonsNormal` had no case in the roster switch**, and it sits in Hive's normal pool — so an act-2 run that drew it did not fight it wrongly, it threw `ArgumentOutOfRangeException` out of `CombatFactory`. **`TestSubject`** is a Glory BOSS whose monster was on `extract_data.py`'s exclusion list next to `BigDummy` and the battle-friends, so `KE.TestSubject = 10007` named a row in `Enemies.g.cs` that did not exist. Its HP is `MinInitialHp => FirstFormHp` — a named property rather than a literal — which the extractor's two regexes both missed, so unexcluding it alone gave a boss with ZERO HP; it follows one level of indirection now. Neither was reachable until the pools became real (E82), which is the point: **a coverage list built from what the CODE has cannot see what the game deals that the code has not got.** `EveryPoolEncounterBuildsTests` is built from the pools instead. |
| E86 | (no capture yet) three Hive monsters looped where the game settles | read from four move state machines | `MoveIndex % n` is the wrong shape for a machine whose `FollowUpState` points at ITSELF. **Tunneler**: BITE → BURROW → BELOW, and BELOW repeats — `% 3` walked it back to the bite every fourth turn, at a third of the damage. **Bowlbug Nectar**: THRASH → BUFF → THRASH2, and THRASH2 repeats — `% 3` gave it a second Strength buff on turn four that the machine has no edge for. **Thieving Hopper**: THIEVERY → FLUTTER → HAT_TRICK → NAB → ESCAPE, and ESCAPE repeats — `% 5` sent a Hopper that had already left back round to steal again. Its FLUTTER is a bare `BuffIntent` besides, which carries no number and no power (its `IsHovering` only picks sound effects), so the Slippery 5 the emulator announced AND applied was invented outright. Three more Deadly-branch damages went with it. |
| E87 | (no capture yet) the Bowlbug Rock stunned itself every other turn | read from `ImbalancedPower` | The Rock's HEADBUTT follows up to a `ConditionalBranchState`, not to a dizzy turn: it headbutts EVERY turn and owes one only when **its own attack was fully blocked**. `ImbalancedPower.AfterDamageGiven` fires on `result.WasFullyBlocked` and sets `IsOffBalance`; the same move then stuns it, and the DIZZY that follows clears the flag. Modelled as an unconditional alternation, the Rock dealt HALF its damage against a player who never blocked, and announced a stun that was not coming. `EnemyState.OffBalance` carries it — a field rather than a buff, because only the Bowlbug Rock has the power and on anything else it stuns outright. |
| E88 | (no capture yet) the Exoskeletons' opener was a roll, and their branch offered a move it does not have | read from `Exoskeleton.GenerateMoveStateMachine` | A flat `rng.Next(3)` was wrong four ways. The OPENER is a `ConditionalBranchState` on **SlotName** — first skitters, second bites, third enrages, and only a fourth rolls, which is why the weak encounter (three) and the normal one (four) differ in more than count. MANDIBLES then goes **straight to ENRAGE** with no branch. The RAND branch it reaches offers only SKITTER and MANDIBLES, **never ENRAGE**. And both branches are `CannotRepeat`, so after a skitter the roll can only come out mandibles — though the draw still happens, because `RandomBranchState.GetNextState` calls `rng.NextFloat(max)` before it ever looks at the weights, the same rule that makes a one-item ancient pool cost its draw (E65). The weak encounter was also building FOUR exoskeletons, the normal one's roster. |
| E89 | (no capture yet) both Bowlbug encounters rolled their composition off the wrong stream | swept every encounter model's `GenerateMonsters` | An encounter that picks its own monsters draws from `EncounterModel.Rng` — its own seeded stream — and the emulator plumbs that for five encounters. **Fourteen models call it**, and Bowlbugs weak and normal were two of the nine that did not: both rolled off the combat rng instead. The normal one was wrong twice over, using `OrderBy(_ => rng.Next())` where the game makes TWO sequential `NextItem` draws over a shrinking candidate list (`_workerValidCounts` caps each worker at one) — a different draw count and a different distribution. **Four models were still unplumbed** — DecimillipedeElite, PunchOffEventEncounter and both halves of ScrollsOfBiting — and all four are closed by E90. The seven this row originally named were wrong: the audit grep only matched single-line calls, so CorpseSlugs weak and normal and SlitheringStranglerNormal read as bare when all three had been plumbed for some time. |
| E90 | (no capture yet) the Bowlbugs were plumbed for their own stream and never used it | found by resolving E89's list | **The encounter-stream plumbing has two halves and only one of them fails loudly.** A builder can be handed its `encounterRngSeed` and still fall back to the combat rng, because the seed is only ever non-null if the encounter appears in `EncounterRng`'s entry table — so E89 wired both Bowlbug builders, forgot the table, and left them behaving exactly as before with code that reads as fixed. Four encounters that had never been wired are closed at the same time: **DecimillipedeElite**, both halves of **ScrollsOfBiting** (each rolls ONE `NextInt(3)` that offsets every creature's opening move, so a single draw off the wrong stream moves the whole fight), and **PunchOffEventEncounter** (two `NextInt(2, 10)` draws, one per construct's starting HP loss). The entry table is generated now, from the models that actually call `base.Rng` in `GenerateMonsters` and the game's own `StringHelper.Slugify` — verified to reproduce all eight entries that had been hand-transcribed and checked against captures. The guard is the one that catches the silent half: not "does the builder mention the seed" but **"does the roster change when the seed does"**. |
| E91 | (no capture yet) two more folded multi-hits, and what the fold was costing | Hive's normal encounters | **Chomper's CLAMP** is `MultiAttackIntent(ClampDamage, 2)` and **Bowlbug Silk's THRASH** is `MultiAttackIntent(ThrashDamage, 2)`; both were single numbers at their A9 damage. E10's shape a third and fourth time — but this pair showed what the fold actually costs beyond the number. Self-Forming Clay's `AfterDamageReceived` fires per INSTANCE and its power is a Counter, so against the real two-hit clamp it arms twice and pays SIX block, not three. **A folded multi-hit silently under-triggers every per-instance hook in the game**, and three relic tests had been written around the wrong figure. The Silk was also inverted: its machine is built `new MonsterMoveStateMachine(list, moveState2)`, so it opens on TOXIC_SPIT. |
| E92 | (no capture yet) two RandomBranchStates offered moves the game cannot pick | read from `TheObscura` and `HunterKiller` | Both reach a branch every move returns to, and both were `rng.Next(n)` over the full list. **The Obscura's** three arms are all `CannotRepeat`, so the move just performed has weight ZERO — the choice is between the other two, and a flat roll gave it a one-in-three chance of an impossible move. **The Hunter-Killer's** two arms are equal weight, so the bite is a coin flip and not the one-in-three it was; BITE is `CannotRepeat` and PUNCTURE is `CanRepeatXTimes(2)`. **The obvious implementation of "do not repeat" is wrong**: comparing the new intent with `CurrentIntent` fails for anything that buffs itself, because the Obscura's WAIL grants it Strength and what it announces climbs, so a base-damage branch never equals the stored intent again. `EnemyState.LastBranch` remembers the branch's IDENTITY instead. |
| E93 | (no capture yet) three more machines that settle, and a Myte on the wrong beat | read from four move state machines | **Ovicopter**: LAY_EGGS -> SMASH -> TENDERIZER -> a branch back to LAY_EGGS or NUTRITIONAL_PASTE, both of which lead to SMASH — a THREE-cycle whose first slot is one or the other, where `% 4` gave it a fourth turn it does not have. `CanLay` is `living teammates <= 3`, so the eggs it summons decide its own next move. **Myte**: the machine opens on a ConditionalBranchState keyed to SlotName and the second Myte starts on SUCK, which is phase TWO — sharing one `MoveIndex % 3` put it on the first's beat from turn two. Its MoveIndex is seeded with the offset now. |
| E94 | (no capture yet) the Slumbering Beetle slept to a schedule | read from `SlumberPower` | It wakes on a COUNTER, not a turn count. `SlumberPower` is applied at 3 and decrements **two** ways: once per enemy-side turn end, and once per INSTANCE of unblocked damage its owner takes. Counting three turns was right only for a beetle nobody hit — and it sleeps behind Plating, so hitting it is exactly what a player does, which means the emulator woke it late in every fight where the player was doing the obvious thing. A multi-hit attack wakes it faster than one big one, which is E91's rule from the other side. |
| E95 | (no capture yet) the Decimillipede's segments walked their cycle in the wrong order | read from `DecimillipedeSegment` | **Two numberings, and the emulator used one where the other belonged.** `StarterMoveIdx` numbers the moves 0/1/2 = WRITHE/BULK/CONSTRICT, but the FollowUpStates walk WRITHE -> CONSTRICT -> BULK. Advancing the starter numbering as though it were the cycle put every segment's second and third moves the wrong way round — and the Strength and Weak riders with them, which is why they read as swapped now. WRITHE was also `MultiAttackIntent(WritheDamage, 2)` folded into 12, and all three moves sat on the A9 branch. The even-and-unique max HP was already faithful. |
| E96 | (no capture yet) a killed Decimillipede segment stayed dead | read from `ReattachPower` | Every segment carries `ReattachPower` at 25: killed, it spends one turn as DEAD_MOVE and comes back **healed by 25 rather than restored**, which is what separates it from the Fogmog eye's revive — a reattached segment is a hurt one. The guard is the whole fight: `if (!AreAllOtherSegmentsDead())`, so the elite is won only by emptying all three inside one window, and the emulator let it be taken apart one piece at a time. |
| E97 | (no capture yet) the Infested Prism gained no block at all | `InfestedPrismsElite` | Its per-move rider lived in `ApplyBuffIntent` as a flat 22 after "every attack" — and **all four of the prism's moves are attacks**, so that case never ran and the dead code hid two separate bugs. Only RADIATE and PULSATE carry a `DefendIntent`, for RadiateBlock (13/11) and PulsateBlock (22/20, the TOUGH pair and so 22 at A8). Riders on an attacking creature belong in the attack branch. WHIRLWIND was `MultiAttackIntent(WhirlwindDamage, 3)` folded into 18, and all four moves were on the A9 branch. |
| E98 | (no capture yet) the Entomancer's bees were the A9 hit count folded into the damage | `EntomancerElite` | `MultiAttackIntent(BeesDamage, BeesRepeat)` where **BeesDamage is 3 at both levels and the REPEAT is what ascension moves**, 8 at A9 and 7 at A8 — so the folded 24 was the wrong number twice over, exactly the Exoskeleton's SKITTER again (E83). Its spear was on the A9 branch too. Its PHEROMONE_SPIT branch and its opening PersonalHive stack were both already right, which is worth recording: the tests written to check them are what showed the bees were not. |
| E99 | (no capture yet) the Kaiser Crab's two halves were unconnected | `KaiserCrabBoss` | **Both powers that make it a boss were missing.** `SurroundedPower` goes on the PLAYER and starts facing Right, so the Crusher — which carries `BackAttackLeftPower` — attacks from behind for **1.5x**; the player only turns when one half DIES, and turning to face the survivor is what takes the bonus away. The emulator had the multiplier baked into the Crusher's announced damage (14 x 1.5 = 21), which is a number that can never stop being wrong. `CrabRagePower` is the other half: the survivor takes **Strength 6 and 99 block** when its partner dies, so halving the boss is not free. Both are modelled now, and the announced damage is the base. |
| E100 | (no capture yet) the Knowledge Demon pondered forever, the Insatiable thrashed forever | `KnowledgeDemonBoss`, `TheInsatiableBoss` | Both used a bare `MoveIndex switch` with no wrap, so each stuck on its last arm from the fourth or fifth turn on — a boss that repeats one move for the rest of the fight. The demon's real machine is CURSE -> SLAP -> OVERWHELMING -> PONDER with a ConditionalBranchState back to CURSE **while it has cast fewer than three**, which makes moves 0-11 a four-cycle and everything after a three-cycle. The Insatiable's is LIQUIFY once, then THRASH -> BITE -> SALIVATE -> THRASH_2 — and `THRASH_2.FollowUpState` is THRASH, so the cycle really does put **two thrashes back to back**; a reading that tidies them into an alternation is the wrong one. Both bosses also had a folded `MultiAttackIntent` (KNOWLEDGE_OVERWHELMING at 3 hits, THRASH at 2) and sat on the A9 branch throughout, as did the whole Kaiser Crab. |
| E101 | `intent` and every turn: the Kaiser Crab's halves swapped which one hit hard | `QS2GYXRKWN-kaiser-crab-a8-play` | **The first act-2 boss capture, and it corrected the model the decompiled source had only half-given.** `SurroundedPower.BeforeCardPlayed` turns the player to face whatever they TARGET — so the half you are not attacking is the one hitting you for 1.5x, and it changes hands the moment you swing. Reading `AfterDeath` alone (E99) got turn one right and every turn after it wrong. The capture is unambiguous: the Crusher opens announcing **18** for a base 12 while the Rocket opens at its bare **3**, and the instant the player's first card lands on the Crusher the bonus moves to the Rocket for the rest of the fight — 27 for an 18 beam, 49 for a 33 laser. The multiplier also belongs in `IncomingDamage`, not at the point of damage, because the game's READOUT shows the modified number. |
| E102 | `player.hp` turn 3: emulator 0, game 48 | `QS2GYXRKWN-insatiable-a8-play` | **The Insatiable's sandpit was on the wrong creature, which inverted it into an instant loss.** `SandpitPower` is applied to the MONSTER with `Target = player`: a counter starting at 4, ticking down each enemy turn, that KILLS the player at zero — and a Frantic Escape pushes it back up, which is what the six status cards are for. The emulator applied it to the player instead, leaving the enemy's counter at ZERO, so the first Frantic Escape the player used took it to 1 and the next enemy turn emptied it and killed them outright. The card meant to buy a turn was lethal to play. |
| E103 | turn 3: the game could afford a Strike, the emulator could not | `QS2GYXRKWN-insatiable-a8-play` | Frantic Escape's `OnPlay` ends with `base.EnergyCost.AddThisCombat(1)` — on the CARD, so only the copy that was played gets dearer. The emulator counted plays on the PLAYER, so using one escape made every other escape in the deck cost more and quietly drained the turn's energy. `CardInstance.CostBump` carries it per copy now, through the piles, the way the enchantment amount does. Also fixed alongside: the six escapes are added in ONE loop at `CardPilePosition.Random`, three to the draw pile and three to the DISCARD — the discard half was being appended, which spent no shuffle draws and put the cards somewhere the game did not. |
| E104 | (found by building the screen) CURSE_OF_KNOWLEDGE chose for the player | `QS2GYXRKWN-knowledge-demon-a8-play` | **The emulator applied a flat Disintegration 6 and offered nothing.** `ChooseCurse` raises a card screen with two options — Disintegration against MindRot, then Sloth, then WasteAway, a different pair on each of its three casts — and overwrites Disintegration's own amount from `_disintegrationDamageValues`, so it escalates **6, 7, 8**. The emulator took the wrong curse two casts out of three, at the wrong amount two casts out of three, and asked nobody. Modelled as a `PendingSelection` now, the same machinery Headbutt and True Grit use; MindRot (draw fewer), Sloth (a per-turn play cap) and WasteAway (less energy) did not exist at all. This is also why the demon could not be captured live: the game blocks on the screen and the harness had nothing to answer it with. |
| E105 | `battle.enemies` turn 4: emulator `(Buff, 11)`, game `(Attack, 11)` | `QS2GYXRKWN-knowledge-demon-a8-play` | **PONDER declares `SingleAttackIntent` FIRST**, then its HealIntent and BuffIntent, so the readout calls it an attack — E12's rule a third time. Announcing it as a buff told a policy that a turn of damage was a turn of nothing. Retyping it then exposed the second half: the heal-and-Strength rider lived in the BUFF handler, so the correction stopped it firing and the demon stopped growing — the Infested Prism's dead-branch problem (E97) in reverse. Its `PonderStrength` was a flat 3 where the pair is (3, 2), which COMPOUNDS: by the demon's third ponder every slap and every hit of KNOWLEDGE OVERWHELMING read three too high. |
| E106 | `intent` every turn: the Fake Merchant enraged forever from its fourth move | `QS2GYXRKWN-fake-merchant-a8-play` | A bare `MoveIndex switch` with no wrap — E100's shape a third time, in an EVENT fight nothing had ever walked. (The Fake Merchant is in `AllSharedEvents`, "events that don't belong to a specific act", so it is reachable in all three.) The real machine has TWO branch sets: everything returns to RAND_MOVE (swipe / spew / throw / enrage), except THROW_RELIC, which returns to RAND_ATTACK_MOVE and so cannot be followed by an enrage. All branches are CannotRepeat and ENRAGE carries a **cooldown of three moves** — weight zero while it appears in the last three logged moves, which is a different rule from the repeat cap and outlasts it. SPEW_COINS is `MultiAttackIntent(2, 8)` folded into 16, THROW_RELIC's Frail rider was never applied, and swipe and throw were both on the A9 branch. |
| E107 | `player.hp` every turn: the Mysterious Knight announced 21 and dealt 15 | `QS2GYXRKWN-mysterious-knight-a8-play` | **An act-2 fight, not an act-1 one** — its event, `TheLanternKey`, is in Hive's pool. **A workaround for one bug that became a bug when the bug was fixed.** The attack path SUBTRACTED the Flail Knight's Strength from its base damage, because its intent table carried damage with the Strength already folded in — the old `Attack 23` is RamDamage 17 plus the Mysterious Knight's Strength 6. Correcting the table to real base values left the subtraction in place, so it announced correctly and hit for six less. The knight's machine was wrong too: RAM first, then a RandomBranchState all three moves return to, with PER-BRANCH repeat caps — WAR_CHANT once, FLAIL and RAM twice each — where the emulator ran a fixed `% 3` cycle. FLAIL is `MultiAttackIntent(FlailDamage, 2)` folded into 20. |
| E108 | `intent` turn 2: emulator `(Defend, 37)`, game `(Buff, none)`; and the Tunneler could never be interrupted | `QS2GYXRKWN-tunneler-and-chomper-a8-play` | **`BurrowedPower` is the whole Tunneler fight and none of it was modelled.** BURROW_MOVE declares its `BuffIntent` FIRST and its `DefendIntent` second, so the readout is a bare buff — E12 a fourth time. What it applies matters more: `ShouldClearBlock` returns FALSE for its owner, so the 37 does not clear between turns and the Tunneler sits behind it (a live capture holds exactly 92 HP for three turns running); `AfterBlockBroken` stuns it into DIZZY_MOVE and then back to BITE_MOVE, which is the ONLY exit from BELOW_MOVE since that follows up to itself; and `AfterRemoved` takes the rest of the block with it. The emulator cleared the block every turn and hit from below forever however hard it was struck. |
| E109 | (no capture yet) the Tunneler's Chomper clamped where it should screech | `TunnelerNormal` | `TunnelerNormal` sets `chomper.ScreamFirst = true`, which makes SCREECH the machine's INITIAL state instead of CLAMP — so this Chomper opens on the screech and its whole alternation runs opposite to a plain one. The emulator gave it no move offset, so it clamped first and stayed inverted for the rest of the fight. The act-2 Chompers encounter already offsets its second chomper the same way; this one was simply missed. |
| E110 | (audit worklist) Glory's three weak encounters, every monster wrong | `QS2GYXRKWN-devoted-sculptor-a8-play` | The first batch worked from `audit_enemy_moves.py`. **Devoted Sculptor**: SavageDamage on the A9 branch. **Turret Operator**: both UNLOADs are `MultiAttackIntent(FireDamage, 5)` folded into a single 20, and the turret handed ITSELF 25 block at creation and another 25 on every reload — that block is `RampartPower`, which lives on the LIVING SHIELD and grants at the start of the PLAYER's turn, so the shield's death was costing the player nothing. **Living Shield**: SHIELD_SLAM's follow-up is a ConditionalBranchState on its ally count — slam while the turret lives, SMASH (which self-loops) once alone — where the emulator slammed once and smashed forever, three times the damage while its partner was up. Its SmashDamage was A9 too. |
| E111 | (audit worklist) the Scroll of Biting ran a cycle where the game runs a chain | `ScrollsOfBitingWeak` | CHOMP -> MORE_TEETH -> CHEW, and CHEW's follow-up is a RandomBranchState over CHOMP (CannotRepeat) and CHEW (at most twice running) — so the chain restarts only through CHOMP. `MoveIndex % 3` made it a fixed three-cycle in the wrong ORDER besides. ChompDamage was A9, CHEW was `MultiAttackIntent(ChewDamage, 2)` folded into 12, and **PaperCuts fired on the scroll's BUFF turn**: `AfterDamageGiven` costs the player max HP when the scroll lands UNBLOCKED damage, so it belongs to the attack. `AfterAddedToRoom` also gives PaperCuts to EVERY scroll, where the emulator gave it to the first three — leaving the normal encounter's fourth without it. `StarterMoveIdx` is now carried in its own field, because it numbers the moves 0/1/2 = CHOMP/CHEW/MORE_TEETH, which is a third ordering again. |
| E112 | (audit worklist) the Axebot booted up every third turn, and its uppercut applied nothing | `AxebotTests` | The machine is BOOT_UP -> HAMMER_UPPERCUT, and from there HAMMER_UPPERCUT <-> ONE_TWO forever — but its INITIAL state is HAMMER_UPPERCUT unless `_stockOverrideAmount.HasValue`, which only a respawn sets. So a bot that opens a fight never boots up at all, where `MoveIndex % 3` walked it back through BOOT_UP every third turn and put ONE_TWO before the uppercut besides. ONE_TWO is `MultiAttackIntent(OneTwoDamage, 2)` folded into a single 20; BootUpBlock, OneTwoDamage and HammerUppercutDamage were all on the A9 branch; and HAMMER_UPPERCUT's WeakPower(2) and FrailPower(2) were simply absent — the Axebot had no attack rider of any kind. `BootUpStrGain * (2 - StockAmount)` was a flat 4 per stock spent. |
| E113 | (audit worklist) the Globe Head's slap announced 21 for a move that hits for 14 | `GlobeHeadTests` | Index 0 had been given THUNDER_STRIKE's folded total as well as index 1, so SHOCKING_SLAP announced 21 where ShockingSlapDamage is 14 — and its FrailPower(2) was never applied. THUNDER_STRIKE is `MultiAttackIntent(ThunderStrikeDamage, 3)`, three hits of 7 rather than one of 21. GalvanicBurstMove's StrengthPower(2) sat in `ApplyBuffIntent`, and **all three of the Globe Head's moves are attacks**, so it never ran — the Infested Prism's dead branch (E97) again — and had it run it would have fired on all three rather than on the burst. |
| E114 | (audit worklist) the Owl Magistrate's peck landed once for 24 instead of six times for 4 | `OwlMagistrateTests` | `MultiAttackIntent(PeckAssaultDamage, 6)` folded — six per-instance triggers lost, the largest fold in the monster set. VERDICT's VulnerablePower(4) was never applied at all, which also means the scrutiny that follows it announced two thirds of what it deals. ScrutinyDamage and VerdictDamage were on the A9 branch. JUDICIAL_FLIGHT's SoarPower is still unmodelled — see O19. |
| E115 | (audit worklist) the Slimed Berserker's hug announced a Buff where the game announces a Debuff | `SlimedBerserkerTests` | LEECHING_HUG declares `DebuffIntent` before `BuffIntent`, so the readout calls it a Debuff — E12's rule again, and the effect had been put in the buff handler to match the wrong type. Retyping it collided with VOMIT_ICHOR, which is the monster's other Debuff, so the branch has to say which move it is: this is the first monster in the set with TWO debuff moves that do different things. FURIOUS_PUMMELING is `MultiAttackIntent(PummelingDamage, 4)` folded into 20, and PummelingDamage and SmotherDamage were both A9. |
| E116 | (audit worklist) the Aeonglass gained its block on the wrong move, and paid a debuff the game does not apply | `AeonglassTests` | EBB attacks and then gains EbbBlock (a flat 33); the block sat in the buff handler, so INCREASING_INTENSITY gained it and the EBB that owns it did not. INCREASING_INTENSITY declares `StatusIntent` before `BuffIntent`, so it announces as a Debuff whose number is WitherAmount — not the Buff of 2 the emulator reported — and the Strength it takes is `IncreasingIntensityBaseStrength + AdditionalStrength`, a count of the times the move has already run, so it climbs 4, 5, 6 rather than sitting at a flat 4. EYE_LASERS is `MultiAttackIntent(EyeLasersDamage, 2)` folded into 24. The old handler also dealt attack damage on the intensity turn and applied an `EbbPower(3)`: **`EbbPower` exists in the game and nothing in this build applies it**, and `BuffId.Ebb` was read nowhere in the emulator — a debuff the player carried and never paid, transcribed from somewhere the current source does not say. `WitheringPresencePower` is still unmodelled — see O20. |
| E117 | (audit worklist) four more folded multi-hits, in encounters still pending | `audit_enemy_moves.py` | The monsters this batch reached inside encounters it did not finish. **The Lost**: EYE_LASERS folded into 10, A9. **Torch Head Amalgam**: SOUL_BEAM folded into 24 — and TACKLE_4 follows up to BEAM, not back to the opening, so the two full tackles happen ONCE and the fight settles into a three-cycle, where `MoveIndex % 5` handed it the opening pair again every fifth turn at 19 apiece. **Spectral Knight**: SOUL_FLAME folded into 12, and the emulator walked HEX, SOUL_SLASH, then soul flame forever where the game rolls a RandomBranchState — SOUL_SLASH capped at twice running, SOUL_FLAME barred from repeating. **The three Adversaries**: BARRAGE declares its MultiAttackIntent before its BuffIntent, so it announces as an Attack of BarrageDamage twice over and not, as the readout said, as a Buff of the two hits added together; the attack was hand-rolled as two `DealAttackDamage` calls inside `ApplyBuffIntent`. Nothing in the game's encounter set builds an Adversary, so they are modelled but unreachable. |
| E118 | (audit worklist) **the Queen never attacked**: she burned bright forever | `QueenTests` | Her machine is PUPPET_STRINGS -> YOU_ARE_MINE -> a ConditionalBranchState on whether the Torch Head Amalgam has died. While it lives she loops on BURN_BRIGHT_FOR_ME; once it is dead the branch sends her to OFF_WITH_YOUR_HEAD -> EXECUTION -> ENRAGE. The emulator ran the first two moves and then took the burn-bright arm unconditionally, so **three of her six moves were unreachable and the fight had no damage in it after turn two**. OFF_WITH_YOUR_HEAD is `MultiAttackIntent(OffWithYourHeadDamage, 5)` and ExecutionDamage was A9. YOU_ARE_MINE also reached the same debuff branch as PUPPET_STRINGS and handed out ChainsOfBinding 99 where the move is Frail, Weak and Vulnerable at 99 apiece — the Slimed Berserker's two-debuff collision (E115) a second time, in the other direction. `Queen.AfterDeath` replaces an ALREADY ANNOUNCED burn-bright with an enrage, which is Ravenous's mid-turn intent change (E19) and needed a hook of its own. |
| E119 | (audit worklist) the Soul Nexus walked a fixed cycle and called its attack a debuff | `SoulNexusTests` | All three moves return to one RandomBranchState whose branches are weight 1 and CannotRepeat, so every turn after the opening SOUL_BURN is a flat roll over the two moves it did not just make; the emulator ran `MoveIndex % 3` and never touched the AI stream. MAELSTROM is `MultiAttackIntent(MaelstromDamage, MaelstromRepeat)` folded into 28. DRAIN_LIFE declares `SingleAttackIntent` before its `DebuffIntent`, so it announces as an Attack — E12 again — and had been typed Debuff, telling a policy that a 19-damage turn was a debuff turn. Every number was the A9 branch. |
| E120 | (audit worklist) the Test Subject respawned for free, and its claw was folded AND off by one | `TestSubjectTests` | **The respawn costs a turn.** `AdaptablePower.AfterDeath` calls `TriggerDeadState`, which puts the subject in a RESPAWN_MOVE marked `MustPerformOnceBeforeTransitioning` — it sits at 0 HP, cannot be hit, announces the Heal-and-Buff, and heals on its next enemy turn. The emulator healed it the instant it fell and announced an attack, so the player lost the free turn the kill buys. That needed `NoPrimaryEnemyLeft` to stop counting it dead: `AdaptablePower.ShouldStopCombatFromEnding` returns true, and it is the only power in the set that overrides that hook — a reattaching Decimillipede segment deliberately does NOT hold the fight open. **Second form**: MULTI_CLAW loops on itself and each performance adds a hit, announced as `11 * (3 + max(0, LastMove))` — folded, at the A9 damage, and off by one, since LastMove started at -1 and the first two claws both read three. `PainfulStabsPower`, which puts a Wound in the discard for every hit that lands unblocked, was a bare phase marker; the hand-rolled attack path the form took would not have triggered it in any case. **Third form**: PHASE3_LACERATE folded into 33, BURNING_GROWL typed Buff where it declares `StatusIntent` first, and its Burns going to the player's HAND rather than the discard. `NemesisPower` toggles Intangible at the end of every enemy turn and was modelled as a permanent Intangible. Both respawn heights and EnrageAmount were ascension-blind. |
| E121 | (audit worklist) a reattached Decimillipede segment resumed its cycle where the game rolls | `DecimillipedeTests` | DEAD_MOVE -> REATTACH_MOVE -> the machine's RandomBranchState, whose three arms are all CannotRepeat — so a segment that comes back does NOT pick the cycle up where it fell, it rolls, and that roll is a draw on the AI stream the emulator was not making. The branch is reached from nowhere else in the machine, which is why nothing else exposed it. `EnemyState.RollsNextMove` carries it, kept apart from `StartsOnBranch` because one is about the machine's INITIAL state and the other about being forced through a state whose follow-up is a branch. The repeat rule is scored against the last move PERFORMED, not the one the segment was announcing when it fell — that turn never happened. |
| E122 | (audit worklist) the Frog Knight's cycle was transposed and its biggest move unreachable | `FrogKnightTests` | TONGUE_LASH -> STRIKE_DOWN_EVIL -> FOR_THE_QUEEN -> a ConditionalBranchState back to the lash, unless the knight is below half HP and has not charged yet: then BEETLE_CHARGE -> TONGUE_LASH. The emulator ran a flat `% 3` with STRIKE_DOWN_EVIL and FOR_THE_QUEEN **the wrong way round**, so every Strength the knight took landed a turn early, and **BEETLE_CHARGE was unreachable** — 35 damage the fight never saw. TongueLashDamage, StrikeDownEvilDamage and BeetleChargeDamage were all A9, PlatingAmount was the ToughEnemies branch used at every level, and TONGUE_LASH's FrailPower(2) sat in `ApplyDebuffIntent` where none of the knight's intents can reach it — the fourth rider found in the wrong branch (E97, E105, E108, E113). |
| H19 | `audit_enemy_moves.py` was over-reporting three different ways, and one of its comments was wrong | the audit itself | Not an emulator divergence — a harness one, and it had sent a reader at correct code. **(1)** "The emulator never touches rng" was a regex for `rng.`, and the emulator rolls through `PickBranch(eligible, rng)` far more often than it calls a method on the stream: three monsters were reported as walking a fixed order while rolling on the line below. **(2)** A branch state that is DECLARED counts, whether or not anything points at it — the Phrog Parasite's RAND is in the state list and unreachable, its two moves following up to each other, so the emulator's fixed alternation was right and the audit called it a defect. **(3)** `MoveIndex %` matched inside COMMENTS, so **a fix that documented what it replaced re-flagged itself** — the Mawler was reported for three batches on the strength of the comment explaining it no longer walks a cycle. Separately, `PickBranch`'s own docstring claimed `rng.Next(n)` draws differently from the game's weighted walk. It does not: `Rng.NextFloat(max)` is `(float)(NextDouble() * max)` and `MegaRandom.Next(max)` is `(int)(NextDouble() * max)`, both one draw, agreeing on every roll that is not an exact integer — checked over 400,000. Six correct call sites were nearly rewritten on the strength of that sentence. |
| E123 | (audit worklist) the Myte never grew, and the matriarch's block was ascension-blind | `MytesTests` | SUCK_MOVE declares its attack and then a BuffIntent — `StrengthPower(SuckStrength)` on itself — and **nothing applied it**. The Myte's "suck" is a plain per-move Strength, not the per-HIT `SuckPower` the Fossil Stalker carries, and only the stalker was ever given `BuffId.Suck` — so a Myte announced the same three numbers for the whole fight where the game's climb every third turn. Found by reading a `[shape]` flag that turned out to be about the opening and not the cycle: the flag was a false positive and the monster underneath it was not. Alongside it, `Slash2Block` is `GetValueIfAscension(ToughEnemies, 14, 12)` and the Lagavulin Matriarch used a flat 14, which is right at A8 and wrong at every level below. |
| E124 | (audit worklist) the Mecha Knight charged every fourth turn, and its WINDUP did nothing at all | `MechaKnightTests` | HEAVY_CLEAVE follows up to the FLAMETHROWER, not to the opening, so CHARGE happens ONCE and the fight is a three-cycle after it; `% 4` brought a 25-damage charge back every fourth turn and put the whole cycle out of step. WINDUP declares `DefendIntent` before `BuffIntent`, so it announces as a Defend of 15 — it was typed Buff, and **there is no Mecha Knight case in `ApplyBuffIntent`**, so the move gained neither its block nor its `StrengthPower(5)`: a third of the knight's turns were spent doing nothing whatsoever, and every attack after one was five low. ChargeDamage and HeavyCleaveDamage were both A9. |
| E125 | **`IntangiblePower` was applied in eleven places and read in none** (was O21) | `IntangibleTests` | It caps everything that takes HP off its owner at 1. Six PLAYER cards apply it — Wraith Form, Shadow Step and four more — as do the Soul Fysh's INTANGIBLE move and the Test Subject's Nemesis toggle, and **nothing anywhere read it**: the buff was carried and never paid, the same defect as the Aeonglass's invented `BuffId.Ebb` (E116) at two orders of magnitude. The whole suite stayed green when the cap was added, because in 2,074 tests nothing had ever asserted it. Both of the power's hooks land on 1 and both are modelled: `ModifyDamageCap` runs inside `Hook.ModifyDamage` under the `Cap` flag — which `ModifyDamageHookType.All` includes, so it governs what block absorbs AND what the intent READS, since `AttackIntent.GetSingleDamage` runs the move through the same hook; `ModifyHpLostAfterOsty` is the backstop for HP taken by a route that is not an attack. Poison is caught by the first rather than the second, `PoisonPower` running its own damage through `ModifyDamage(..., All, ...)`. It decrements at the end of the ENEMY side turn whoever owns it, and being a Buff it gets none of the skip-a-tick grace `PowerCmd.Apply` gives a debuff landing on the player. **The Nemesis alternation is a bool, not a stack**: reading "is Intangible present" to decide the flip is the obvious shortcut and is wrong, because Intangible's own tick has already removed it by the time Nemesis looks — so the toggle re-applied every round and the Test Subject stayed permanently untouchable, which is what it did before this too. |
| E126 | (audit worklist) six monsters on the A9 branch, and three of them wrong a second way | `FabricatorTests`, `KnightsTests` | The tail of `audit_ascension_literals.py`. **Fabricator**: FabricatingStrikeDamage on the A9 branch, and its ConditionalBranchState on `CanFabricate` was ignored entirely — the emulator rolled FABRICATE against FABRICATING_STRIKE unconditionally, so a Fabricator with four bots up summoned into a full board and **DISINTEGRATE was unreachable**. **MagiKnight**: MAGIC_BOMB follows up to RAM, not to the opening, so POWER_SHIELD and DAMPEN happen ONCE and the rest is a three-cycle; `% 5` brought both back every fifth turn. Its three damage numbers were A9 and PowerShieldBlock used the ToughEnemies value at every level. **Stabbot**: STAB_MOVE declares its attack BEFORE its debuff, so it announces as an Attack — the emulator had it as a Debuff of 12 with the attack as its SECONDARY, the announcement inverted, and 12 is the A9 damage. The `[types]` check could not see it, because `SecondaryIntentFor` does say Attack for this monster and that check asks per MONSTER rather than per move. **Parafright**, **ToughEgg** and **Zapbot** were plain A9 literals; the last two live in `BotIntent`, a switch expression the ascension audit could not read at all until H20. |
| E127 | `SoarPower` did nothing, and the reason it was deferred was a misreading (was O19) | `OwlMagistrateTests` | JUDICIAL_FLIGHT applies `SoarPower(1)` and VERDICT removes it; it halves POWERED attack damage against the owl. It was recorded as unmodelled on the grounds that "powered" was a distinction `BuffSystem.IncomingDamage` could not make. It is not: `ValueProp.IsPoweredAttack()` is `Move && !Unpowered` — attack damage from Attack cards and from creatures attacking — and `IncomingDamage`'s only two callers are `EnemyAI.DealAttackDamage` and `CardEffects.DealDamageToEnemy`, which are exactly those. Everything unpowered goes through the `DealUnpoweredDamage*` helpers and never arrives. **The function IS the powered-attack path**, so the hook had a home all along and the deferral cost three batches. |
| E128 | `VitalSparkPower` and `TaintedPower` were both unmodelled | `InfestedPrismsTests` | The Infested Prism arrives holding `VitalSparkPower(VitalSparkAmount)` and PULSATE stacks another on top, so the count climbs through the fight. Every Skill the player plays stamps them with `TaintedPower`, which ADDS that much to every powered attack against them and removes itself outright at the end of the enemy turn. It is the Skill-card twin of the Globe Head's Galvanic — modelled the same way, off the largest spark on the board rather than by tracking an affliction per card instance. Found through a FALSE flag: the audit reported VitalSparkAmount's 3 as an A9 literal, and the 3 in the prism's block is WhirlwindRepeat, a hit count — the failure mode the audit's own docstring warns about. Reading it found a missing power rather than a wrong number. |
| H20 | `audit_ascension_literals.py` could only see `case KE.X:` arms | the audit itself | A harness defect, and the same one the move audit had (H19): a checker that reads code has to be able to see all of the code. The emulator writes a monster's numbers in at least four shapes — case arms, `if (enemy.DefId == KE.X && ...)` riders inside the shared attack and defend branches, switch-expression arms like `SecondaryIntentFor` and `BotIntent`, and `if (defId == KE.X)` inside a helper — and this collector read only the first. It reported the Myte, the Knowledge Demon and the Skulking Colony as carrying bare A9 literals while the guarded `Ascension.Value(...)` sat in a rider ten lines up, and it could not see the Zapbot's and Stabbot's real ones at all, because those live in a switch expression. Every rider added since the Hive batches was invisible to it. |
| E129 | The Forgotten's dread was a constant where the game's climbs, and killing it was worth nothing | `LostAndForgottenTests` | `DreadDamage` is not a property with a value, it is a property with a BODY: `GetValueIfAscension(Deadly, 15, 13) + its own DexterityPower`. MIASMA hands it two Dexterity every other turn, so the dread climbs 15, 17, 19 — and the emulator's flat 15 was right for exactly ONE turn at A8, by the coincidence of 13 + 2. That is also why the ascension audit never flagged it: its regex looks for the one-line `=> GetValueIfAscension(...)` form and a property with a body does not match. Under it sat `PossessSpeedPower`, unmodelled: it tallies every point of Dexterity The Forgotten takes and **gives all of it back when it dies**, so killing it should undo the debuff and instead did nothing. |
| H21 | the enemy-death `else if` chain had grown two riders that do not belong in it | `CombatEngineTests` | Not a divergence — a latent one. `HandleEnemyDeaths` runs a chain of revive branches where `else if` means "this creature came back, stop looking", and two independent on-death side effects had been threaded into the middle of it: the Queen's enrage hook (added with E118) left `TryRespawnTestSubject` guarded on the dead creature not being a Torch Head Amalgam, which is true today and would stop being true the moment anything else wanted a hook there. Both are lifted out above the chain now, with a comment saying why the chain is a chain. Nothing behaved wrongly; it was one edit away from doing so. |
| E130 | five Silent cards discarded the leftmost card where the game asks the player | `DiscardChoiceTests` | Survivor, Acrobatics, Dagger Throw, Prepared and Hidden Daggers all raise `CardSelectCmd.FromHandForDiscard`, and all five called `DiscardFirstCardsFromHand` — the same picking-for-the-player as the Knowledge Demon's curse screen (E104), and choosing what to throw away is the entire point of Survivor. They open `CardSelectionKind.DiscardFromHandRepeated` now, which reopens until its picks are spent or the hand empties, so upgraded Prepared asks twice. **Hidden Daggers was wrong three further ways**: its two vars are `CardsVar(2)` — how many to discard — and `Shivs = 2`, and the emulator was dealing the CardsVar as DAMAGE the card does not have, reading the upgrade as a third Shiv when upgrading in fact leaves the count at two and upgrades the Shivs themselves, and discarding nothing at all. The Shivs are created after the discard resolves, which is what the selection's follow-up field is for: a Shiv made before the screen opened would be a candidate for the discard that made it. |
| E131 | **two card keywords the extractor never emitted**, both read by the engine (O22, and Retain) | `SlyTests`, `RetainKeywordTests` | `extract_data.py` collects canonical keywords into a tuple — `("Ethereal", "Exhaust", "Unplayable")` — and `CardDef` has fields for `Retain` and `Sly` that were not in it. **A keyword the extractor never emits reads exactly like a card that does not have it**, and both were live: `CardInstanceExtensions.IsRetained` decides what survives the end-of-turn hand discard, so ELEVEN cards that should stay in hand were thrown away every turn; and Sly decides what auto-plays when an effect discards it, which is the whole point of Silent's discard theme. Regenerating with the two added changed exactly 19 card rows and nothing else. |
| E132 | `CardKeyword.Sly` was unmodelled, so Hand Trick was seven block and nothing else | `SlyTests`, `HandTrickTests` | A Sly card discarded BY AN EFFECT is played rather than joining the pile — `CardCmd.DiscardAndDraw` gathers the sly ones as it moves them and auto-plays each once the rest are down. Eight cards carry the keyword and Hand Trick grants it to a chosen Skill for a single turn, which is the second half of that card and did nothing at all. Routed through ONE chokepoint, `DiscardMovedCards`, because the discard sites live in two different dispatches — the `case SI.X:` arms and the by-NAME fallback — which is exactly the shape that gets a rule applied to half of them. **The end-of-turn hand discard deliberately does not come through it**: that cleanup is not a `CardCmd.Discard` in the game either, so holding a Tactician to the end of the turn is not free energy, and a test says so. |
| E133 | answering a card-selection screen put anything it queued a whole action late | `SlyTests` | `CombatEngine.Step` RETURNED from the selection branch instead of falling through, so the auto-play drain at the bottom never ran for it. Nothing queued from a selection before Sly, which is why it went unnoticed — but answering a discard screen with a Sly card queues it to play, and the game plays it as part of that discard rather than after whatever the player does next. Found by a test that asserted the energy had arrived and got the old value. |
| E134 | (extractor sweep) **three more keyword gaps of the same family, and an upgrade direction nothing modelled** | `UpgradeKeywordTests` | Prompted by E131: if two keywords could go missing from `extract_data.py`'s tuple, what else? A field-by-field sweep of all five generated tables found **no other never-emitted field** — the narrow question was clean. Counting each keyword against the decompiled source was not. **Retain is GAINED on upgrade by twelve cards** and `CardDef` had no `RetainWhenUpgraded`, so an upgraded Calculated Gamble was discarded with the rest of the hand; the Innate pair exists precisely because that attribution matters, and Retain needed the same. **Exhaust is REMOVED on upgrade by nineteen cards** and `ShouldExhaustAfterPlay` read the printed keyword alone, so an upgraded Discovery, Hologram or Secret Technique exhausted anyway — which for most of them is the upgrade's entire benefit. **Ethereal is REMOVED on upgrade by three**, and the emulator knew one of them by its RAW ID: `!(def.Id == 159 && card.Upgraded)` is Echo Form and said nothing about Apparition or Void Form. The direction is not interchangeable, and a check that only asked whether the keyword appears in `OnUpgrade` would have read the two groups as the same thing and got one backwards. |
| E135 | (extractor sweep) `CardKeyword.Eternal` had no field at all, so a run could delete Ascender's Bane | `UpgradeKeywordTests` | `CardModel.IsRemovable` is `!Keywords.Contains(CardKeyword.Eternal)` and `CardSelectCmd.FromDeckForRemoval` filters on it, so the game will not so much as OFFER an Eternal card for removal. Seven curses carry it. The emulator's own comment beside Precise Scissors already named the rule — "Ascender's Bane, a card the game will not even offer for removal" — and the filter was never written: that pass fixed the emulator choosing FOR the player and left what it was allowed to choose FROM. |
| H22 | `scripts/audit_card_keywords.py`, so this family cannot reopen | the audit itself | Counts every card keyword the decompiled source declares against what `Cards.g.cs` emits, in both directions, and fails with a non-zero exit when they disagree. Verified against the original defect: removing Retain, Sly and Eternal from the extractor's tuple and regenerating makes it report all three and exit 1. It also flags a keyword that starts changing on upgrade when `CardDef` has no field for that change — the shape of E134, one patch ahead of the next reader. |
| E136 | Silent's three "approximation" poison cards were each a DIFFERENT card | `AccelerantTests`, `CorrosiveWaveTests`, `OutbreakTests` | All three carried an `approximation` comment, and none of them was a rough version of the real thing. **Accelerant** was modelled as Envenom stacks: `AccelerantPower` does nothing on its own, `PoisonPower` reads it and re-triggers — Envenom poisons on an attack, Accelerant makes poison already applied tick again. **Corrosive Wave** was a one-shot Poison AND a Weak the card never applies; `CorrosiveWavePower.AfterCardDrawn` poisons every hittable enemy each time its owner DRAWS, and `AfterSideTurnEnd` removes the power, so it is a one-turn draw engine. **Outbreak** was Noxious Fumes at a bigger number; `OutbreakPower` counts every Poison its owner applies and every THIRD one deals 11/15 to all enemies. A comment saying "approximation" says how much the author knew, not how close the result is. |
| E137 | poison triggered once a turn whatever Accelerant said | `AccelerantTests` | `PoisonPower.AfterSideTurnStart` triggers `min(Amount, 1 + Accelerant)` times, dealing the CURRENT amount each time and decrementing after each — so with Accelerant 1 a poison of 5 deals 5 and then 4, and the stack falls by two. The emulator dealt the stack once and decremented once, which is right at zero Accelerant and is why it looked fine. The clamp matters too: it can never trigger more often than there is poison to spend, which the game's own comment calls out. |
| E138 | Bouncing Flask piled every bounce on one enemy and never touched the target stream | `BouncingFlaskTests` | `TargetType.RandomEnemy`, and the card rolls `Rng.CombatTargets.NextItem(HittableEnemies)` for EVERY bounce. The emulator called `ApplyEnemyDebuff`, which takes the aimed-at enemy — so three bounces of 3 Poison became 9 on one creature, and three draws the live game makes were never made. The same failure target choice had before `TargetRng` existed. |
| E139 | `ApplyAllEnemyDebuff` repeated the chokepoint's body instead of calling it | `OutbreakTests` | It duplicated `ApplyEnemyDebuffToTarget` — the same three lines — so everything hanging off "a debuff was applied to an enemy" fired through one path and not the other. Outbreak counts Poison there, so poison from a card fed its counter and poison from Noxious Fumes did not. Found by a test asserting a power's poison counts, which the game hooks precisely because it hooks the EVENT rather than the card. The roster is copied before the loop now as well: Outbreak's burst can kill from inside it. |
| E140 | six more Silent "approximations", each a different card | `BurstTests`, `MasterPlannerTests`, `SpeedsterTests`, `MurderTests`, `TrackingTests`, `WraithFormTests` | **Burst** stacked OneTwoPunch — the same rule for ATTACKS — so it doubled the wrong half of the deck; `BurstPower.ModifyCardPlayCount` adds a play to a SKILL. **Master Planner** drew a card next turn; the power applies the Sly KEYWORD to every Skill its owner plays, permanently for the combat, so the copy plays itself the next time anything discards it — implementable only now that Sly exists (E132). **Speedster** gave energy next turn; `AfterCardDrawn` deals 2 to every enemy for each card drawn MID-turn. **Murder** dealt a flat 25/35; its damage is `CalculationBase(1) + ExtraDamage(1)` per card drawn this COMBAT. **Tracking** applied Vicious, an unrelated power; `TrackingPower.ModifyDamageMultiplicative` returns its own amount against a Weak target, so Tracking 2 is DOUBLE damage, and a second copy adds one rather than two. **Wraith Form** charged its Dexterity once on play; `AfterSideTurnStart` takes it every turn, which is the price the card is built around. |
| E141 | Burst doubled ITSELF, because the play count was read after the card resolved | `BurstTests` | `ModifyCardPlayCount` is consulted when the play is SET UP. The emulator read the buff after `CardEffects.Apply`, and Burst is a Skill that grants Burst — so playing one at 2 stacks left 3. Read before the card resolves now. **`EchoForm` and `SignalBoost` are still read after it** and would self-double the same way if a Power granted SignalBoost or a card granted EchoForm; neither exists today, so this is recorded rather than changed — the ordering wants checking against the game before it is moved. |
| E142 | two of the eleven are `MultiplayerOnly` and unreachable in a solo run | `FlankingTests`, `SneakyTests` | Flanking and Sneaky carry `CardMultiplayerConstraint.MultiplayerOnly`, so `CardFactory.FilterForPlayerCount` drops them from the pool before the rarity roll and no single-player run can hold one. Their approximations cost nothing. Pinned as tests rather than rewritten, so the next reader working the approximation list does not spend a batch on them. Note the filter lives in `RunRewardGenerator.IsAllowedSolo` and NOT in the pool table — the pool still lists them, which is where the game filters too. |
| E143 | Phantom Blades did neither of the two things it does | `PhantomBladesTests` | `PhantomBladesPower` gives the Retain KEYWORD to every Shiv the player owns, and `ModifyDamageAdditive` pays its amount (9/12) to a Shiv attack only while NO Shiv play has finished this turn — the FIRST Shiv of the turn, once. The emulator stacked InfiniteBlades, which adds a Shiv each turn: not a smaller version of the card, a different one. |
| E144 | the Shiv's damage was dealt from two places and the bonus reached one | `PhantomBladesTests` | Shiv is handled both by a by-NAME arm and by the shared base-damage path, and the two had drifted. Adding Phantom Blades' bonus to the shared path left the card hitting for its printed value, because the by-name arm is the one that runs. Collapsed into one `ShivDamage` helper. **The third instance this session of a rule reaching half its call sites** — after the discard sites (E132), the debuff sites (E139) and the keyword tuple (E131), all four the same shape: a duplicated body instead of a shared chokepoint. |
| E145 | Serpent Form poisoned where the game strikes | `SerpentFormTests` | `SerpentFormPower` records its amount BEFORE each card the player plays and spends it after, dealing that much unpowered damage to a random hittable enemy. The record-then-spend pair is what stops a card played before the power existed from triggering, and what stops the Serpent Form that applied it from triggering itself. The emulator stacked NoxiousFumes, which poisons every enemy each turn. |
| E146 | The Hunt's kill earned nothing | `TheHuntTests` | `TheHuntPower` is a visual marker and nothing else; the behaviour is in the card, and it is the entire reason to play one. A Hunt that KILLS its target adds a whole extra `CardReward` of three to the room. The emulator dealt the damage and stopped. Carried on the combat as `ExtraCardRewards` and converted at reward generation, exactly as a Heist's `StolenBackGold` is — a combat cannot reach the reward generator. The offer's draw ORDER against the ordinary reward is unverified; see O23. |
| E147 | Haze applied the wrong debuff to the wrong number of creatures | `HazeTests` | Its var is `PowerVar<PoisonPower>(4m)` and it applies Poison to every hittable enemy. The emulator applied Weak 2/3 to ONE. Found by reading a card with no `approximation` comment on it — which is the finding: the label was never what marked the risk. |
| E148 | Bullet Time applied NoBlock where the card applies NoDraw | `BulletTimeTests` | `NoDrawPower`, and the comment on the emulator's own line said "prevent draw" while the line applied `BuffId.NoBlock`. So a card that reads as a combo enabler — the whole hand free for a turn — played as a self-debuff that stopped the player blocking and left them drawing freely. |
| E149 | Anticipate handed out PERMANENT Dexterity | `AnticipateTests` | Its var is a `PowerVar<DexterityPower>`, but the card applies `AnticipatePower`, which derives from `TemporaryDexterityPower` and is handed back at the end of the turn. Reading the VAR rather than the Apply made a 0-cost common a permanent buff. **A card's vars name the numbers, not the powers** — the two disagree here on purpose, and this is the second card this session where they did (Outbreak's `OutbreakPower` var is read by name too). |
| E150 | Expose consumed one Artifact charge where the card removes the power | `ExposeTests` | `PowerCmd.Remove<ArtifactPower>` takes the whole power off, and it happens BEFORE the Vulnerable so the debuff always lands. `TryConsumeArtifact` spends a single charge, so a two-Artifact enemy kept one — which then swallowed the very Vulnerable the card exists to apply, making the card do nothing at all against exactly the enemies it is for. |
| E151 | Malaise ignored its upgrade and made its Strength loss temporary | `MalaiseTests` | `if (base.IsUpgraded) powerAmount++`, which the emulator did not read at all — an upgraded Malaise spent X and applied X. And the loss is a plain `StrengthPower(-powerAmount)`: permanent, where the emulator used the temporary variant that is handed back at end of turn. |
| E152 | Mirage blocked a flat number where the card blocks the board's poison | `MirageTests` | `CalculatedBlockVar` with a multiplier of the total PoisonPower across living enemies, base 0 and extra 1 — the block IS the poison, and the upgrade is a cost cut. The emulator gave a flat 10/14, a guess at the average, which is both wrong and backwards: the card is worthless in a deck with no poison and enormous in one built on it. |
| E153 | Grand Finale was PLAYABLE with cards left in the draw pile | `GrandFinaleTests` | `IsPlayable => PileType.Draw.GetPile(owner).Cards.Count == 0` is a playability rule and belongs in the action mask. The emulator checked it inside the effect and dealt nothing otherwise, so the play was allowed, the card and the energy were spent, and **an agent was offered an action the real game does not have** — the worst shape of this defect class, since a policy has to learn not to take it. `CombatEngine.IsPlayableNow` carries it now. |
| E154 | Blur was Barricade: a 1-cost common playing as a 3-cost rare, permanently | `BlurTests` | `BlurPower.ShouldClearBlock` returns false for its owner so the block survives the turn start, and `AfterSideTurnStart` DECREMENTS the power — a counter of 1 saves the block once and is then gone. The emulator applied `BuffId.Barricade`, which is the Ironclad rare whose block never expires at all and never decrements. So one Blur made every point of block the player gained for the rest of the combat permanent, and every Defend after it compounded. The two rules differ only in the decrement, which is exactly why they cannot share an id. The card's Blur var is a flat 1 at both levels; the upgrade raises the BLOCK. |
| E155 | Fan of Knives wore its own Shiv count as a different card's power | `FanOfKnivesTests` | `FanOfKnivesPower` does nothing on its own: the SHIV card reads it and returns `TargetType.AllEnemies` instead of `AnyEnemy`, so every Shiv the player holds hits everything. It is `PowerStackType.Single`, so a second copy brings more Shivs and not a second stack. The emulator applied **InfiniteBlades at the Shiv COUNT** — one Shiv every turn forever, which is a different card, carrying this card's own number for its other half — and the retarget was absent entirely. Third card this session whose whole behaviour is "something else checks for this power", after Accelerant (E136) and Master Planner (E140): **a power with an empty body is a pointer to the card that reads it**, not a no-op. |
| E156 | Knife Trap gave Thorns where the card replays your whole exhaust pile of Shivs | `KnifeTrapTests` | It takes every Shiv-tagged card out of the EXHAUST pile, upgrades each if the trap is upgraded, and auto-plays them all at the trap's own target. `CalculatedShivs` is a display var counting them; the card has no damage and no block of its own. Thorns 4/6 was not a smaller version of that — it was a different card, and one that scales with nothing. The replayed Shivs exhaust again as they resolve, so the pile ends where it started, and the upgrade is on what it THROWS rather than on how many. |
| E157 | every auto-played attack rolled a target and threw the result away | `HellraiserTests` | `AutoPlayCore` computed `targetIndex` from `Rng.CombatTargets` and never read it, so the roll happened — correctly, since `CardCmd.AutoPlay` rolls when handed a null target — and the card then hit the first living enemy regardless. Invisible against a single creature and wrong against several, in the target AND in nothing else, since the stream draw was already being made. Found because Knife Trap is the one card in the emulator that hands an auto-play an EXPLICIT target, which forced the question of what the others do. `HellraiserTests` asserted all 15 damage on `enemy[0]`; it now asserts 9 on the aimed-at enemy and 15 across the board, which is what the game does. |
| E158 | Nightmare duplicated the leftmost card in hand, three times, immediately | `NightmareTests` | Two defects and each one was the whole card. `CardSelectCmd.FromHand` with a null filter means the player CHOOSES what to copy — a Nightmare that takes whatever happens to be first is a coin flip rather than a plan, the same picking-for-the-player as E104 and the five discard cards. And `NightmarePower.BeforeHandDraw` hands the clones over at the start of the NEXT turn and then removes itself; the emulator handed them over on play. Paying three energy for cards you cannot use this turn is what the three energy buys, so delivering them early makes a rare card strictly better than it is. The upgrade only does `EnergyCost.UpgradeBy(-1)`: the count is a literal 3 at both levels, where the by-name fallback beneath the switch read it as 2 rising to 3. |
| E159 | Echoing Slash threw one volley where the card re-throws itself per kill | `EchoingSlashTests` | `while (attackCount > 0) { attackCount--; ...; attackCount += hits.Count(r => r.WasTargetKilled); }`. Every creature a volley kills buys another volley, and those can kill in turn, so against a row of small enemies it cascades — three dying in sequence is four volleys, and the survivor eats all four. The emulator hit everything once under a comment saying the real card "gains damage per kill", which is a different card: the damage never changes, the number of volleys does. A comment describing a mechanic the code does not implement is worse than no comment, because it reads as a known simplification rather than as a bug. |
| E160 | Blade of Ink made plain Shivs, and its fallback granted Focus | `BladeOfInkTests` | `foreach (... Shiv.CreateInHand(...)) CardCmd.Enchant<Inky>(item, 1m)` — the enchantment IS the card. Without it Blade of Ink is a strictly worse Blade Dance, one fewer Shiv at the same cost, which is exactly what the emulator played; and the by-name fallback underneath the switch had the card granting **Focus**, a Defect mechanic on a Silent rare. `Inky` had no entry in the `Enchantment` enum at all. Its two numbers are its own vars — a `DamageVar(1m)` and a `PowerVar<WeakPower>(1m)` — and NOT the amount it was applied at, which is why it declares `ShowAmount => false`: an Inky Shiv is 5 damage and Weak 1 however the enchantment was granted. First modelled enchantment whose payload lands on an enemy rather than on its owner. |
| E161 | Untouchable added the draw pile to its block, which the card never mentions | `UntouchableTests` | `OnPlay` is one `CreatureCmd.GainBlock` on a `BlockVar(6m)` and nothing else. The emulator added `state.DrawPile.Count` on top — another ten block or more early in a fight, on a 2-cost common, growing with the deck for the whole run. **An invented scaling term is worse than a wrong constant**: a constant is wrong by a fixed amount you could spot in one capture, and this was wrong by a different amount in every fight and every turn of every fight. |
| E162 | Ricochet put every hit on the aimed-at enemy instead of rolling per hit | `RicochetTests` | `TargetingRandomOpponents`, and `AttackCommand.Execute` rolls `Rng.CombatTargets.NextItem(validTargets)` INSIDE its per-hit loop, recomputing the living targets each round. The emulator used `DealDamageMultiHit`, which lands every hit on one creature — so a card that sprays five hits across the room did 15 to a single enemy, and the target stream was never drawn from, which desynchronises every roll downstream of it. Identical to Bouncing Flask's defect, and the second card found taking `RandomEnemy` to mean "the enemy you aimed at". The damage never upgrades; `RepeatVar(4)` does. |
| E163 | Predator's upgrade drew a third card it does not draw | `PredatorTests` | `PowerCmd.Apply<DrawCardsNextTurnPower>(..., 2m, ...)` is a literal at both levels — `OnUpgrade` raises the DAMAGE and nothing else. The emulator read the upgrade onto the draw as well. Same shape as Hidden Daggers' invented third Shiv and Leading Strike's (which was right): **the upgrade belongs to whichever var `OnUpgrade` names, and a card with two vars upgrades one of them unless it says otherwise.** Sucker Punch, in the same batch, upgrades both — so the rule has to be read per var rather than guessed per card. |
| E164 | Dodge and Roll owed next turn its PRINTED block rather than what it gained | `DodgeAndRollTests` | `decimal amount = await CreatureCmd.GainBlock(...)` and `PowerCmd.Apply<BlockNextTurnPower>(..., amount, ...)` — the command returns `modifiedAmount`, with Dexterity and Frail already in it and clamped at zero, and the card pays out from THAT. The emulator applied `Blk(def, upgraded, card)`, which is right only when nothing is modifying block: under Dexterity 3 it was three short next turn, under Frail one over. `CardEffects.GainBlock` now returns what it gained, mirroring the command it models. The grant itself is `ValueProp.Unpowered`, so Dexterity does not apply a second time — verified rather than assumed, since the emulator was already right about that half. |
| E165 | Scare shared Haze's case body, so it applied Poison instead of Weak | `ScareTests` | Two Silent skills that look alike were given one `case` arm, and the body belonged to the other one: a 0-cost card whose whole text is Weak 1 to every enemy was handing out 4 Poison. Scare's Weak is a literal `1m` inside the loop with no var behind it, and `OnUpgrade` is `RemoveKeyword(CardKeyword.Exhaust)` — so the upgrade changes what the card COSTS you, not what it does. **Merged case labels are a distinct failure mode from a wrong number**: the fall-through reads as a deliberate "these two are the same card", and nothing about it looks like a bug on the way past. |
| E166 | Leg Sweep applied a whole extra stack of Weak at both levels | `LegSweepTests` | `PowerVar<WeakPower>(2m)` with `UpgradeValueBy(1m)` is 2/3; the emulator gave 3/4. The block half of the same card was right, which is what makes this the ordinary kind of wrong — one var read from memory instead of from the source. |
| E167 | Up My Sleeve was a Shiv short and never got cheaper | `UpMySleeveTests` | `CardsVar(3)` +1, not 2/3. And `base.EnergyCost.AddThisCombat(-1)` at the end of `OnPlay` is the card's whole shape: a 2-cost that becomes a 1-cost and then a free one, for the rest of the combat. The comment beside the line said the cost "drops in combat in real game" and no code did it — **a comment that describes the divergence is not a fix, and it stops the next reader from finding one**, the same way Echoing Slash's "gains damage per kill" did (E159). The bump rides on the CARD through the piles, as Frantic Escape's does in the other direction, so a second copy is still full price. |
| E168 | Escape Plan peeked at the draw pile instead of reading what it drew | `EscapePlanTests` | `(await CardPileCmd.Draw(...)).FirstOrDefault()` and a type check on the card that came back. Peeking at `DrawPile[0]` is the same card only while the pile is non-empty: with an empty pile the draw reshuffles the discard and the peek describes a card that is no longer there, and at the hand limit the draw returns nothing while the peek still says Skill. Wrong in both directions, and only in the states a long fight actually reaches. |
| E169 | Infinite Blades made its Shiv after the draw rather than before it | `InfiniteBladesTests` | `InfiniteBladesPower.BeforeHandDraw`. The emulator added its Shivs at the END of the turn-start sequence. Invisible until the hand limit bites, and then it inverts which cards get cut: the game fills the slot with the Shiv and shortens the DRAW, the emulator drew first and dropped the Shiv. A hook's name is a specification — `BeforeHandDraw` had to be modelled before the draw and was not. |
| E170 | Well-Laid Plans kept the WHOLE hand for a turn instead of one chosen card forever | `WellLaidPlansTests` | `WellLaidPlansPower.BeforeFlushLate` raises a card-selection screen over the hand — min 0, max Amount, filtered to cards not already retaining — and gives each pick `GiveSingleTurnRetain()`. It is a POWER, so it asks every turn for the rest of the combat. The emulator applied `BuffId.RetainHand`, which keeps the entire hand and counts DOWN: 1 meant "keep everything for one turn, then nothing". Two different rules that cannot share an id, exactly as Blur could not share Barricade's (E154). Three firsts came with the fix: the first selection raised OUTSIDE a card play (the end turn is parked and resumed by whichever answer finishes the screen), the first that may be DECLINED (minimum zero, so the skip is offered one past the last candidate), and the first single-turn Retain grant. `BuffId.WellLaidPlans` is appended rather than filed beside `RetainHand` because the observation writes `(int)buff.Id` straight into the vector — inserting one renumbers every buff after it and silently rewrites every committed fixture. |
| E171 | Shadow Step gave Intangible where the card doubles your damage next turn | `ShadowStepTests` | `ShadowStepPower.AfterSideTurnStart` applies `DoubleDamagePower` and then removes itself, so discarding your hand buys a turn of doubled card damage — landing a turn late, which is the cost. The emulator gave Intangible 1: a DEFENSIVE buff where the card is offensive, and IMMEDIATE where the card is delayed. Both halves point an agent the wrong way — "discard your hand to survive this turn" and "discard your hand to kill next turn" are opposite plans built out of the same card. `DoubleDamagePower` returns a flat `2m` however many stacks it holds, so a second Shadow Step buys a second TURN rather than quadruple damage. |
| E172 | Shadowmeld was whole-hand Retain where the card doubles a turn's block | `ShadowmeldTests` | `ShadowmeldPower.ModifyBlockMultiplicative` returns `2^Amount` for its owner and `AfterSideTurnEnd` removes it. The emulator applied `BuffId.RetainHand`. **Second Silent rare in the same sweep standing in for RetainHand**, after Well-Laid Plans (E170) — when a generic buff is close enough to two different cards, it will be reached for a third time, and the fix is that the specific rule gets its own id. The hook does not read `props`, so it doubles UNPOWERED block as well; it lives in `CardEffects.GainBlock` rather than `IncomingBlock` for that reason, which is what lets it double an Afterimage in the same turn. |
| E173 | Afterimage paid out on its own play, and Dexterity raised the block | `AfterimageTests` | `AfterimagePower.BeforeCardPlayed` records the power's amount for the card about to be played and `AfterCardPlayed` spends THAT. The power's own Data comment gives the reason: "avoid triggering on cards that started play before it was applied, and avoid gaining extra block on multiple plays of After Image". The emulator read the amount after the card resolved, so the first copy blocked for itself and the second blocked 2 rather than 1 — **the same defect as Burst, and the third time a play-count or amount has had to be read before the card resolves rather than after**. The block is also `ValueProp.Unpowered`, which the emulator's powered `GainBlock` was scaling by Dexterity. |
| E174 | Tools of the Trade discarded the leftmost card instead of a chosen one | `ToolsOfTheTradeTests` | `AfterPlayerTurnStart` raises `CardSelectCmd.FromHandForDiscard` with a `CardSelectorPrefs(prompt, Amount)` — the single-count constructor, so min equals max and the discard is compulsory while the CHOICE is the player's. Drawing one more and pitching your worst is the whole engine; pitching whatever happens to be leftmost is closer to a downside. Seventh card found picking for the player, after Headbutt, True Grit, the Knowledge Demon's curse, the five discard cards and Nightmare. This one raises its screen at TURN START and simply leaves it standing — nothing is owed afterwards, so `ValidActions` restricts to it until it is answered, unlike Well-Laid Plans' which has an end turn parked behind it. |
| E175 | the Defend TAG was carried at one call site, so two Defend cards lost it | `DefendSilentTests` | `FastenPower.ModifyBlockAdditive` pays out only when the block's `cardSource` is tagged `CardTag.Defend`. The emulator passed that as an `isDefend` flag from a single place — the generated-card approximation — so the four Defends that route through it were right and the two with their own `case` arm, `DefendSilent` and `UltimateDefend`, silently were not. The tag is a fact about the CARD, not about the call that gained the block, so it now rides on the state for the duration of the card and every arm that blocks honours it. **Sixth instance of one rule applied at half its call sites**, after the discards, the debuffs, the keyword tuple, the Shiv damage and the temporary-strength helper — and the fix is the same one every time: hoist the fact to where it belongs and delete the parameter. The name stands in for the tag and here it is exact: the six cards whose names contain "Defend" are precisely the six the source tags. |
| E176 | Glass orbs drew cards where the orb is a decaying area attack | `GlassOrbTests` | `GlassOrb.Passive` deals its value to every hittable enemy and then knocks its own `_passiveVal` down by one, floored at zero; `EvokeVal => PassiveVal * 2m`. The emulator drew ONE card on the passive and TWO on the evoke — an invented effect on an orb whose whole identity is that it wears out, so a Defect holding a Glass orb got a draw engine instead of a damage source that decays. The evoke's draw also ran off a fresh `new Random(0)` rather than the combat's stream. The decay is per-ORB state, which is why `OrbState` grew a `PassiveValue` beside Dark's `EvokeValue`. |
| E177 | Lightning orbs evoked at the aimed-at enemy instead of rolling | `LightningOrbTests` | `ApplyLightningDamage` with a null target rolls `Rng.CombatTargets.NextItem(hittable)`, and the evoke passes null. The PASSIVE already rolled and the evoke did not, so against several creatures every evoke landed on the same one and the target stream went undrawn — desynchronising every roll after it. Third card-or-orb in this sweep found reading "random" as "the one you pointed at", after Bouncing Flask (E139) and Ricochet (E162). |
| E178 | Dark orbs started six higher under Focus than they should | `DarkOrbTests` | `DarkOrb._evokeVal = 6m` is a bare field initialiser and `EvokeVal => _evokeVal` has no `ModifyOrbValue` on it, while `PassiveVal => ModifyOrbValue(6m)` does. So Focus raises what a Dark orb ACCUMULATES and not what it starts with; the emulator seeded the base with Focus too. Under Focus 3 a freshly channelled Dark orb was worth 9 rather than 6, and the error rode along in every subsequent evoke. Focus reaching one half of an orb and not the other is not an oversight in the source — Plasma declares plain literals at BOTH ends and is untouched by Focus entirely. |








---

## Fixed — harness

These produced no wrong emulator behaviour but hid or misattributed real bugs.
They are catalogued because each one cost more than the engine bug it concealed.

| # | Gap | Consequence |
| --- | --- | --- |
| H1 | The replay compared `state_type` but never WHICH event | Two runs sitting in different events both read `"event"`, so an Underdocks divergence stayed invisible for 60 steps and surfaced as a gold mismatch. `event.event_id` is now a default compared field. |
| H2 | Target ids were treated as stable | The game **renumbers** entity ids as enemies die — four gardeners are `_0.._3`, and once the first dies the survivors become `_0.._2`. The map was built once on entering a fight, so a replay quietly attacked the wrong enemy for the rest of it. Values are now ordinals among living enemies, resolved against the emulator's own list. |
| H3 | Hands were compared by card id alone | An upgrade does not change a card's id, so Defend and Defend+ compared equal. A lost upgrade surfaced three steps later as HP — which reads like a damage bug, and **I misdiagnosed it as one**, blaming an enemy whose damage was correct. With `(id, upgraded)` the same bug reports at step 63 instead of 77, naming the card. |
| H4 | The snapshot carried far more than it compared | Block, energy, max_hp, relics and potions were summarised and thrown away, and the deck was not summarised at all. Two engine bugs (E10, E11) surfaced on the first run of the widened comparison. |
| H5 | `validate_real_game_trace.live_enemy_intent` special-cased two monsters | It returned Debuff when the capture plainly said `Attack '8'` — the expected value **bent to agree with the emulator**, which is the one thing a live fixture must never do. Removed, and the fixture re-captured rather than hand-edited. |
| H6 | `combat_sweep --play` derives actions from the emulator's state | Any change to the emulator's roster order walks the two sides out of step and the capture fails outright. Needs the H2 treatment: resolve targets by what they name. Currently blocks confirming E16. |
| H7 | The target map spelled a hyphenated enemy differently from the game | `build_target_map` slugified the display name by folding only WHITESPACE, so "Two-Tailed Rat" came out `TWO-TAILED_RAT` where the game's entity id says `TWO_TAILED_RAT`. The lookup missed — and `translate_target` does not fail on a miss, it falls back to the entity id's numeric SUFFIX, which is the position among LIVING creatures and stops matching the emulator's index the moment anything dies. That is precisely the trust H2 removed, restored by a hyphen. The map now keys on the capture's own `entity_id` as well, which needs no transcription at all. |
| H8 | The tracer treated an ACCEPTED action as a done action | The retry loop only ever caught an action the game REFUSED. The other failure strands the capture: `proceed` from a shop came back `ok`, the map screen really did open with the right options on it, the travel vote registered in the game's own log — and no room ever loaded. The state settles on `unknown`, every later action is refused, and the capture ends holding a run that is still alive. It is a race, not a rule (the committed traces all leave their shops fine), which is why it needed handling rather than avoiding. `recover_stranded_run` now nudges with a `proceed` and re-posts until the run has actually moved, recording nothing until it takes. The first capture taken with it needed six attempts at exactly that step and would otherwise have died on floor 5. |
| H9 | The abandon crash wedged the game, and the harness blamed the clock | `SaveManager.DeleteCurrentRun` deletes `current_run.save.backup` unconditionally and `CloudSaveStore.DeleteFile` THROWS when it is absent. The exception escapes `NAbandonRunConfirmPopup.OnYesButtonPressed` half way through: the run is gone from disk but the main menu never finishes coming back, reporting `menu_screen: main` with no enabled buttons, forever. Every capture after that dies on "Timed out waiting for menu screen 'main'" — a message that had already sent one investigation after the lobby instead of the state it names (see HANDOFF's embark notes). The workaround was known and written down as a manual step, which is exactly how it stayed a gotcha; `ensure_run_save_backup()` now does it before every abandon. `wait_for_menu` also went from 10s to 60s, because an abandon that saves and then preloads 126 assets does not reliably fit in ten. |
| H10 | An unbounded "proceed until the map" loop turned a divergence into a hang | `commands.py` walked `while phase != PHASE_MAP`, posting a proceed each time. When the emulator sat on a screen the reference had already left, the proceed changed nothing, the loop posted it again, and the replay spun at 99% CPU forever — reading for all the world like a hang in the ENGINE, which is where the first hour of looking went. It is bounded now, and gives up when a proceed does not change the phase, so the caller reports the mismatch it actually has. Same lesson as H8: a harness that cannot make progress must say so rather than keep trying. |
| H11 | Every card-select screen was assumed to be the toggling kind | A card-select phase is TWO screens wearing one name. An offer grid (`FromChooseACardScreen`) resolves on the click — the game says "Choosing card: X" and leaves — while a selection over the DECK toggles and waits for a confirm. The replay only ever modelled the second, because **no committed trace had ever replayed a grid**: Brain Leech and Room Full of Cheese roll them, and no capture had reached either. So the answer was deferred waiting for a confirm that never came, and the emulator sat on the screen while the reference walked on. It asks the run which screen is open now (state list 17, run API v16) rather than reading the mod's message text — the same reason target ids are resolved against the emulator's own roster instead of being parsed out of a name. |
| H12 | Neither side of the harness could answer a screen wanting TWO cards | The capture policy picks a card by priority and the screen reports **no selection state at all** — its cards carry an index and nothing to say one is already ticked. So a two-card screen picked the same card every time, toggling it on and off; and because a toggle DOES change the snapshot, the settle-wait never timed out and the capture hung outright rather than failing. The replay had the mirror of it: `select_card` held one deferred index and "a later toggle replaces an earlier one", so a two-card answer applied one card. Both now track the set. The replay applies them **highest index first**, because a removal takes its card out of the deck as it goes and a lower index applied first shifts every index above it. Precarious Shears ("Choose 2 cards to Remove") is the first blessing that asks for two, which is why neither gap had ever been met. |
| H13 | `battle.enemies` step 81: reference intent `None`, emulator `8` | `P5E6EWCMDW-a8-stonehumidifier` | **The comparison was under-reading the reference.** `_attack_intent` counted an intent only when its type was the literal `"Attack"`, and a Gas Bomb announces `("DeathBlow", "8")` — `DeathBlowIntent` derives from `SingleAttackIntent` and carries real damage in the same label format. So the reference reported "not attacking" and the two sides disagreed about nothing. Compared like any other attack now, which makes the check STRICTER: the emulator's announced 8 has to match. Worth remembering that a divergence can be the harness declining to look. |
| H14 | `battle.enemies`: reference empty, emulator holds four Wrigglers | `buff-smoke`, `3PFLW9XC5D-a8-buff200` | **The capture sampled a frame mid-resolution.** A Phrog Parasite's death removes it and then awaits `CreatureCmd.Add` for each of its four Wrigglers, and `is_actionable_state` was happy with a combat that had a player turn, a play phase and a hand — but no enemies at all. So the snapshot recorded against the killing blow is a fight against nobody, and the Wrigglers appear a step late with their HP otherwise matching exactly. An empty enemy list inside a COMBAT state is never settled: a fight with nothing left to kill has already become a rewards screen. It is a settle condition now. Note this is the OPPOSITE reading to E23, where the emulator really did keep a finished fight open — the tell is what the reference does NEXT. |
| H15 | `battle.enemies` step 168: game ended the fight, emulator has a Fogmog left at 4 HP (was **O11**) | `7WGQ2VNJ4M-a8-buff400` | **The target map was emptied for exactly the attack that ends a combat.** A capture pairs an action with the state it PRODUCED, so the killing blow of a fight is recorded against the rewards screen it opened — and the map was gated on THAT step's state type, which is no longer a combat one. With no map, `FOGMOG_0` fell through to the entity id's suffix, `0`, and the emulator's index 0 was the eye standing in front of the Fogmog. The gate is now the PREVIOUS step's enemy list, which is the state the action was actually chosen in. Note how well it hid: the map is only ever missing on the last action of a fight, and only matters when something is standing in front of the target, so a Fogmog's illusion is close to the one place in act 1 it could show. The catalogued suspicion — `resolve_targets` reading a def id where it wanted HP — was wrong; slot 0 IS current HP, which `summarize_battle` reads the same way. |
| H16 | (no capture yet) two of `translate_target`'s three paths never resolved their answer | found while reading the code behind H15 | Only the `target_map` path was resolved against the emulator's enemy list. The entity-id suffix and the `combat_id` lookup both returned the GAME's numbering — an ordinal among living, since the mod lists only `IsAlive` enemies — and it was handed to `env.step` as an absolute index. The emulator keeps its dead where the game removes them, so the two agree exactly until something dies. `build_target_map`'s own docstring names the hazard for the suffix and the fix stopped at spelling the names right; the fallback itself stayed unresolved. Every path returns an ordinal now and `execute_command` resolves once, so no path can skip it. |
| H17 | the Knowledge Demon's capture replayed a move behind at its far end | `QS2GYXRKWN-knowledge-demon-a8-play` | **The RL step limit was truncating a fixture REPLAY.** `Sts2CombatEnv` defaults to `MAX_EPISODE_STEPS = 50` to stop a training policy looping forever, and a capture that plays cards spends about four actions a turn — so a fourteen-turn boss fight is ~56 and the offline replay stopped around turn twelve, reading as an emulator a move behind rather than as a replay that had been cut off. The sweep never saw it because it ignores `truncated` and drives the native combat directly. Diagnosed wrongly at first as the terminal turn, where the live player dies partway through; the boundary was fine and the cap was not. `test_every_turn_intents_match`'s `"fight ended early"` is what made it loud rather than silent — a replay that stops short otherwise compares only the turns it reached. No committed fixture had crossed 50 before this one. |
| H18 | the EVENT fights could not be captured at all | `debug_start_encounter` | `ModelDb.AllEncounters` is the union of each ACT's pools, so an encounter only an event starts is not in it — the Fake Merchant, the Architect, the Battleworn Dummies, Dense Vegetation, the Mysterious Knight, the Punch Off. **These are not all act 1**, which is how they were first described here: `DenseVegetation` is Overgrowth's and `PunchOff` is Underdocks', but `TheLanternKey` (the Mysterious Knight) is HIVE's, `BattlewornDummy` is GLORY's, `FakeMerchant` is in `AllSharedEvents` and belongs to no act, and `TheArchitect` is in no event pool at all — `RunManager` enters it directly at an act boundary. The mod looked encounters up by scanning that list, so every one of them answered "Unknown encounter" and the whole set was unreachable however the sweep was called. It falls back to the registry by `ModelId` now — the category slug for `EncounterModel` plus the class name through `StringHelper.Slugify` — which is where those models have been all along. Four of the six were captured within minutes of the fix, and two of them diverged. |





---

## Patterns worth remembering

**Compare contents, not containers.** H1, H2, H3 and H4 are the same mistake four
times: comparing `state_type` without which event, enemy slots without which enemy,
card ids without which version, a summary without the fields it carried. Each let a
real divergence sit silently until it surfaced somewhere unrelated.

**A check that passes can still be scoped wrong.** E20 slipped past the encounter
sequence comparison because `encounter_names()` normalises the `WEAK`/`NORMAL`
suffix away *on purpose* — for most encounters the split really is one roster. E21
slipped past the deck comparison because it is a multiset.

**Anything left to a default is a silent allow.** E5 is the archetype:
`IsEventAllowed` ends in `_ => true`, so an untranscribed rule does not fail, it
quietly permits. `generate_event_gating_coverage.py` now makes an untranscribed
rule a build failure.

**Neow is not a thing.** All 28 of its options are `RelicOption<T>` — obtain the
relic, then `Done()`. E3, E17 and E18 all presented as "Neow bugs" and were all
relic-effect bugs. `MassiveScroll` is in the game's `PositiveOptions` but its
`IsAllowed` is `Players.Count > 1`, so leaving it out of a solo run is correct.

**The soak and the suite catch different things.** E7 was a crash the 1666-test C#
suite could not reach, because it needs two draws from one event in one run.

**The stream is a run-level object, so the fight that pays is not the fight that
spends.** E23 is the archetype: twelve extra draws in a floor-12 fight surfaced as a
wrong opening hand on floor 14, and every read of floor 14 was a read of the wrong fight.
Anything that shares state across combats — `Rng.Shuffle`, `Rng.Niche`, `monster_ai` —
has to be measured where it is spent, not where it hurts.

**A turn the game never takes is not free.** The same entry: the emulator "just" played
out one extra player turn after a fight was already over. It drew cards, so it reshuffled,
so it moved a run-level stream. Any code path that continues past a terminal state is a
divergence in waiting, whatever it looks like it costs.

**A range over an enum is a promise about declaration order that nothing enforces.** E41
sat correct for as long as nobody appended to the enum, and then went quietly wrong —
not with an error, but by answering "no" for one elite out of six. Anything shaped like
`>= First and <= Last` over a list other people extend wants to be an explicit set, and
the test wants to name every member rather than sample one.

**A capture is evidence only up to the point the game stopped behaving.** The Precarious
Shears run wedged partway through, and its last three steps record a `monster` state with
an empty enemy list and a Burning Blood heal already applied — a broken game, not ground
truth, and the emulator was right where the "reference" was wrong. It is committed cut to
its trustworthy prefix. Read the tail of any capture whose run did not end cleanly before
believing a divergence in it.

**A green fixture set measures the fixtures, not the emulator.** The nine traces went
clean, and the tenth — taken for no reason except that it would hold a relic none of them
held — diverged on its first combat reward and produced four defects. Three of those had
nothing to do with the relic; they sat on paths no earlier capture happened to walk. When
the set goes green the honest reading is "these runs are exhausted", and the next capture
is worth more than any amount of re-reading them.

**A fudge factor outlives the thing it was compensating for.** E30's four burned Rewards
calls were put there to paper over E29's wrong stream. Fixing the real defect left the
fudge behind, still wrong, now in the opposite direction — and the catalogue already had
the same story for Kaleidoscope's eighteen. Any "advance the stream by N to stay aligned"
is a debt with a name attached; write down which defect it is standing in for, so that
whoever fixes that defect knows to delete it.

**An early `break` is a scope decision nobody wrote down.** E27 and E28 are the same
line: `if (Hits > 1) { ...; break; }` silently decided that a multi-hit attack skips
everything below it, which included one monster's own debuff and the whole of Flame
Barrier. The branch that dealt the damage should not get to decide which effects the
damage has — anything that answers a hit belongs in the helper that lands the hit, next
to Thorns. When adding a `break` to a long `if` chain, the question is not "does my case
work" but "what did I just exclude".

**Depth is where the untested code is, and buying it is cheap.** The first three buffed
captures found three defects between them (E58, E59, H14) after thirty unbuffed traces had
gone green. E58 is the sharpest case: the emulator has always rolled an elite's relic
before its cards, and no committed trace had EVER claimed a combat relic reward — the only
three relic claims in thirty runs are Neow screens on step 2. A whole reward screen was
unreachable by the scripted player, so a whole ordering was unchecked. When the fixture set
goes green, ask what the runs never got to do, not just what they never held.

**Zero is not the same as nothing.** E55 returned a flat 0 gold for one encounter, which
was right about the amount for the case somebody had observed and wrong about the DRAW —
the game rolls the reward and multiplies by a proportion, so the value is spent whatever
the proportion is. That put every rewards-stream value after that fight off by one, and it
surfaced four steps later as a different card. Whenever a special case answers "none of
this", check whether the general path would still have consumed randomness on the way to
saying the same thing. It is the same lesson as the short-circuit in E48 and the White
Beast Statue comment in `CheckPotionRoll`, arrived at from a third direction.

**The screen the effect opens is part of the effect.** Six blessings now (E43, E44, E49,
E50, E51, and Neow's Bones in E42) turned out to be the same defect: the game asks the
player something and the emulator answered for them. It is worth treating as the DEFAULT
suspicion for any relic or event whose text contains a choice — "upgrade a card", "remove
a card", "gain a relic" — because the wrong answer is only half the cost. The other half
is the step: a screen is an action the run takes, so an emulator that skips it is
permanently one decision ahead, and everything downstream reads as a state-machine
divergence rather than as the missing prompt it is.

**A branch that leaves early has to leave the same way the others do.** E53's skip path
cleared the queue and returned straight to the map, past both of the checks that decide
where a rewards screen goes back to. It was unreachable for Neow — whose rewards cannot
be skipped — so the gap sat in a branch only events could enter, and only when a player
DECLINED something. When two paths exit the same screen, the exit belongs in one function
they both call, not copied into the one you happened to be reading.

**A short-circuit is a decision not to draw.** E48 was one `||`. The game's reward
upgrade draws its float and THEN decides what to do with it, so the draw is part of the
stream whatever the answer; writing the cheap override first meant the expensive term was
never evaluated and the value was never spent. Any boolean built from an override and a
ROLL has to evaluate the roll into a local first — `||` and `&&` are control flow, and
control flow over a random stream changes the stream. The same lesson is already written
into `CheckPotionRoll`, one file away, in a comment saying exactly this about White Beast
Statue.

**A recorded suspicion is not evidence, and it decays.** O11's catalogue entry named a
cause — `resolve_targets` reading a def id where it wanted HP — and it was wrong; slot 0
is current HP, which the same file's `summarize_battle` reads that way three lines apart.
Two more hypotheses of my own died the same way. What settled it in one reading was
**re-capturing the seed**: the trace reproduced the divergence at the same step, and the
cause was visible in it. A capture costs a few minutes and the staring had cost far more,
so when an open entry resists, re-capture before re-reasoning. Write the suspicion down by
all means — but the entry is a place to start, not a finding, and the longer it sits the
more the code around it has moved.

**A resolver only helps on the paths that call it.** H16: `translate_target` had three
paths and exactly one ran its answer through the living-enemy resolution. The other two
returned the game's numbering and it was used as an emulator index — which agrees exactly
until something dies, so the bug is invisible in every fight nothing survives. The fix
that lasts is structural: make every path return the SAME kind of thing (an ordinal) and
resolve once at the single call site, so a new path cannot skip the step. The same shape
is worth checking wherever a "translate" and a "resolve" are two functions.

**A capture pairs an action with the state it PRODUCED.** H15: the killing blow of a
fight is recorded against the rewards screen it opened, so anything gated on "is this step
a combat state?" is empty for exactly the last action of every fight. That is a narrow
window — it needs something standing in front of the target to show at all, which in act 1
is close to just a Fogmog's eye. When a harness reads a step's state to interpret a step's
action, it wants the PREVIOUS step.

**A generated table is a diff; a transcribed one is a divergence.** E81 is E47's lesson
arriving a second time in a different costume. The encounter tags were hand-copied after
E66 had just finished demonstrating what a missing one costs, and they had drifted by four
entries anyway — because there is no moment at which a hand-written table is discovered to
be short. Generating it also turned up E82, which nobody was looking for: act 3's four
encounter pools were a placeholder returning act 2's. **Generating one thing tends to
reveal the next**, because the generator has to be told about every case and the
placeholders are the cases it cannot be told about.

**A hand-kept list of ids is a claim about the game that nothing rechecks.** E47's
`IsNonUpgradableCard` held fourteen entries where the game declares thirty-seven, and it
had presumably been right about whatever prompted each addition. There is no moment at
which such a list is discovered to be short — it fails only on the paths that touch the
missing members. Anything expressible as a property the decompiled source states outright
(`MaxUpgradeLevel => 0`) belongs in `extract_data.py`, generated, where being short is a
diff rather than a divergence a hundred steps downstream.

**Two of the three defects in this round were found by a field the capture compares
almost by accident.** E47 and E48 both surfaced as `player.deck` — a multiset compared at
every step — dozens of steps before they showed up as HP or block, which is what the
batch summary printed first. The summary's "first divergence" lines are per-field and
unordered; reading only the first two got the symptom and not the cause both times. Sort
the divergence list by step before believing where a run went wrong.

**A rule read off one observation is a rule fitted to one state.** E26's "a summoned rat
goes to the FRONT" was true, and true for the reason given — it just happened to be the
answer only while all three starters were alive. `Slots.LastOrDefault` is the rule; the
front was one of its outputs. When a note explains a placement by naming slots, model the
slots.
